## File Extensions in OrcaWave

.yml : data file in ascii format (key-value pairs)
.owd : orcawave data file (binary format)
.owr : orcawave result file (binary format)
.gdf : mesh file in ascii format

