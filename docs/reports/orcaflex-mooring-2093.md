# Mooring qualification implementation checkpoint

Issue: [digitalmodel 2093](https://github.com/vamseeachanta/digitalmodel/issues/2093). Human-facing report: [HTML](orcaflex-mooring-2093.html). **Code complete; operational qualification blocked by licence availability.** One native construction attempt failed before model loading; no retry occurred.

The orchestrator reports 178 tests passed in 19.36 seconds and the legal scan passed. Earlier focused counts below are overlapping development checks and must not be added to this total.

## Verified offline changes

- GeneralBuilder now emits an explicitly empty `RestartStateRecordingTest` default. Other nulls remain null; explicit generic overrides retain their values and types. The targeted regression was RED (1 failed, 20 passed), then GREEN (21 passed).
- The mooring-buoy template comments now describe a single mooring line and regular-wave height/period. Numerical inputs and parsed YAML are unchanged. Original descriptive metadata remains source data, not an independent geometry assertion.
- Offline packaging copies the source, generates a complete seed-0 bundle, hashes all inputs, checks the audited manifest and records recursive differences against the C07 reference. Seven packaging tests pass, including failed-preparation cleanup and retained neighboring files. Unresolved reference differences are not converted into parity claims.

## Corpus evidence

[Corpus JSON](orcaflex-mooring-2093-corpus.json) records 94 source paths, 91 direct generations and two jumper adapter generations. Passing-ship own-schema validation passes; its generation integration remains unknown. The three direct-route rejections remain visible.

Of 534 generated files, 93 change only `RestartStateRecordingTest: ~` to a quoted empty string in `includes/01_general.yml`; 441 are byte-identical. No unexpected generated change occurred. Both sides used `PYTHONHASHSEED=0`; no output normalization was used. Native model construction attempts in this offline corpus run: zero. Generator current-profile warnings remain; byte agreement does not establish engineering adequacy.

The final single-line comment correction followed generation. The JSON retains original audit hashes and separately records the new source hash and verified parsed-YAML equivalence; no second corpus run is claimed.

## Native attempt — licence failure before loading

At `2026-09-11T21:02:05Z`, code `f0f35bd14d9d695948463606c3606fe7408426fa` attempted the candidate under Python 3.11.15 / OrcaFlex 11.6c. `api.Model(threadCount=1)` failed at stage `construct` with `OrcFxAPI.DLLError` 25. FlexNet reported licensed users reached (`-4,132`); the StatFlex feature was missing (`-5,412`), and the HASP fallback found no files. These diagnostics do not establish a model-data failure.

Parent elapsed time: 4.922 seconds. Child exit: 1. Timeout: false. Cleanup state: confirmed. One construction was attempted; zero model loads and zero completed solves occurred. `LoadData`, statics, dynamics, save and independent readback were **not reached**. Requested thread count was one; no constructed model existed for observed-thread verification.

The prepared bundle contained six files; manifest SHA-256 was `63cb2c70becb38d4634e854ee145688ff1bf30c9ef29716f50b2e78964e868f0`. See [native attempt evidence](orcaflex-mooring-2093-native.json) and [reference differences](orcaflex-mooring-2093-reference-differences.json). Offline comparison found 56 unresolved differences, so the original-reference route remains blocked and reference compatibility is false.

The source stages are 11.5 seconds build-up plus 100 seconds main duration. A deadline expiry must be reported as incomplete; it must not silently shorten those stages. Existing generic-line smoke acceptance does not qualify this mooring component.

No retry will occur until the owner confirms a licence-recovery checkpoint: availability and entitlement in the intended execution context, continued approval of the exact candidate/manifest, and no competing managed run. The next authorized attempt must retain the same staged evidence and stop on failure; recovering a licence will not resolve the 56 reference differences or confer engineering parity.

## Limits and handoff

The source remains a single mooring line and mid-line clump supporting one buoy. The checked-in YAML uses Earth-relative damping, while the [Orcina C07 guide](https://www.orcina.com/wp-content/uploads/examples/c/c07/C07%20Metocean%20buoy%20in%20deep%20water.pdf) describes fluid-relative damping and arbitrary example damping. Strength allowables, mean drift and convergence/equilibrium acceptance remain unresolved. No response histories, extrema, CSV engineering outputs or calibration verdict were generated by this failed attempt.

No production queue, scheduled task, environment deployment or native solve was performed by the corpus/packaging lane. Runtime adapter and Windows ownership tests have separate orchestrator evidence. The working source's explicit null overrides are intentionally preserved and can still require case-specific review. Engineering parity with C07, reference tolerances and reusable model-family qualification are not established here.

Private corpus directory and raw logs are retained in the orchestrator handoff, outside this public document. The branch preserves the implementation, corpus audit and failed native checkpoint together; the issue remains open.
