# #1903 generator/artifact brand drift — plan recorded

**Authorization:** operator direction in-session, 2026-07-29 ("continue work using codex
subagents"), given after #1903 was raised as the recommended next task. This is NOT an
owner-reviewed written plan and does not claim to be — the plan comment on the issue says
the same thing.

Plan: https://github.com/vamseeachanta/digitalmodel/issues/1903#issuecomment-5114557083

Scope: HTML template heads only, in 13 of 18 build_*.py generators. No computation, no
numeric constants, no sidecar logic, nothing under docs/.

Acceptance is objective and independently checked: re-running each generator must produce
a zero diff against its committed page. Codex implements; it cannot verify, because the
generators need `uv run`, which does not work in its sandbox. Verification is Claude
regenerating and diffing.
