# Pipe Capacity

Computes API STD 2RD internal-pressure capacity for a 12.75 inch OD X65 subsea export pipeline.

Run:

```bash
uv run python -m digitalmodel examples/workflows/pipe-capacity/input.yml
```

Expected outputs are written to `examples/workflows/pipe-capacity/results/`, including the resolved result configuration `input.yml`.
