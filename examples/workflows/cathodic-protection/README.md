# Cathodic Protection

Computes sacrificial anode demand for a coated offshore steel structure using the ABS guide ship workflow.

Run:

```bash
uv run python -m digitalmodel examples/workflows/cathodic-protection/input.yml
```

Expected outputs are written to `examples/workflows/cathodic-protection/results/`, including the resolved result configuration `input.yml`.
