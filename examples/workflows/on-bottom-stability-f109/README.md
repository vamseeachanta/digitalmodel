# On-Bottom Stability F109 Workflow

Computes DNV-RP-F109 lateral stability for a concrete-coated Gulf of Mexico pipeline using bundled pipe, soil, and metocean inputs.

Run:

```bash
uv run python -m digitalmodel examples/workflows/on-bottom-stability-f109/input.yml
```

Expected output:

- `examples/workflows/on-bottom-stability-f109/results/input.yml`
