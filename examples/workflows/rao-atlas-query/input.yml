basename: parametric_query

parametric_query:
  atlas: rao_tabulation
  point:
    period_s: 8.0
    heading_deg: 45.0
  policy:
    on_out_of_range: escalate
    confidence: required
  output_dir: results

default:
  log_level: INFO
  config:
    overwrite:
      output: true
