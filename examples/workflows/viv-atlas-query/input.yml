basename: parametric_query

parametric_query:
  atlas: viv_analysis
  point:
    span_length_ft: 70.0
    current_speed_in_s: 36.0
  policy:
    on_out_of_range: escalate
    confidence: required
  output_dir: results

default:
  log_level: INFO
  config:
    overwrite:
      output: true
