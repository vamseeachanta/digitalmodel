basename: parametric_query

parametric_query:
  atlas: orcaflex_library
  point:
    case: storm-hs5
    heading_deg: 60.0
  policy:
    on_out_of_range: escalate
    confidence: required
  output_dir: results

default:
  log_level: INFO
  config:
    overwrite:
      output: true
