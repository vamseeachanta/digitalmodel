basename: parametric_query

parametric_query:
  atlas: anchor_capacity
  point:
    diameter_m: 5.0
    length_m: 15.0
    su_kpa: 30.0
    demand_kN: 9000.0
    factor_of_safety: 1.5
  policy:
    on_out_of_range: escalate
    confidence: required
  output_dir: results

default:
  log_level: INFO
  config:
    overwrite:
      output: true
