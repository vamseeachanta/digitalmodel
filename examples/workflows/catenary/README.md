# Catenary

Computes angle-based catenary geometry for a flexible line with 100 m vertical separation and a 30 degree hang-off angle.

Run:

```bash
uv run python -m digitalmodel examples/workflows/catenary/input.yml
```

Expected outputs are written to `examples/workflows/catenary/results/`, including the resolved result configuration `input.yml`.
