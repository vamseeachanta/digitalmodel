basename: response_spectrum

response_spectrum:
  record:
    file: data/elcentro_ns_1940.txt
    units: g
  baseline:
    method: pre_event_mean
    samples: 20
  filter:
    enabled: true
    band_hz: [0.1, 25.0]
    order: 4
  output_dir: results
  generated_label: "(generated by response_spectrum workflow)"

default:
  log_level: INFO
  config:
    overwrite:
      output: true
