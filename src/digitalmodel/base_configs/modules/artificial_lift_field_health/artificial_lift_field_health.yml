basename: artificial_lift_field_health

artificial_lift_field_health:
  # Default solver. 'gibbs' does not transform load (#1857) and must not
  # be used for diagnosis; synthetic cards are forward-modelled to the
  # surface before solving (#1862).
  solver_method: everitt_jennings
  output_dir: results
  worst_wells_limit: 5
  fail_health_states:
    - critical
    - failure
  well_defaults:
    rod:
      diameter: 1.0
      length: 5000.0
    pump:
      diameter: 1.75
      depth: 5000.0
    surface_unit:
      manufacturer: Sim
      unit_type: Test
      stroke_length: 144.0
      gear_box_rating: 640000.0
    spm: 10.0
  wells: []

default:
  log_level: INFO
  config:
    overwrite:
      output: true
