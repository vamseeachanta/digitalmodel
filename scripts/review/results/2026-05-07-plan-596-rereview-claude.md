# Re-review artifact: digitalmodel #596 revised repo-structure plan — reviewer 1

Verdict: MAJOR

Findings:
- [severity: high] B1528 old-path migration is still internally contradictory — The revised plan correctly classifies `outputs/b1528_sirocco/**` as durable committed evidence to move into `docs/examples/b1528-sirocco/generated-reports/**`, but row 7 and the verification section still require updating **all docs/source references** and then running a repo-wide `rg -F 'outputs/b1528_sirocco'`. Live repo state shows current `src/` still intentionally uses that path for runtime/config behavior and report-link generation: `src/digitalmodel/naval_architecture/data/b1528_sirocco_moored_current.yml:49` sets `directory: outputs/b1528_sirocco/moored_current`, and multiple report emitters in `src/digitalmodel/naval_architecture/*` embed `.../outputs/b1528_sirocco/...` GitHub links. The plan also says runtime code may continue generating under ignored `outputs/b1528_sirocco/`. Those statements cannot all be true at once. Required change: explicitly split **durable evidence links** vs **runtime output-directory references**, and lock exactly which source files are allowed/required to change in this issue. If runtime generation stays under `outputs/...`, the stale-reference gate must allow those runtime refs and only forbid obsolete committed-evidence links.
- [severity: medium] The stale-reference verification command is not approval-grade as written — The exact gate is a bare `rg -F 'outputs/b1528_sirocco' --hidden -g '!.git'`, but that command returns success when matches exist, and it currently also matches historical plan/review artifacts that intentionally mention the old path. So the gate is both semantically inverted and over-broad. Required change: replace it with a negative check such as `if rg ... <scoped paths>; then exit 1; fi`, and scope/exclude historical artifacts like `docs/plans/**` and `scripts/review/results/**` or enumerate approved residual references.

Approval readiness: not ready

Prior MAJOR findings are mostly resolved explicitly: the plan now locks the contract/config/checker/test paths, embeds a draft `config/repo_structure.yml`, enumerates all tracked root `*.py` files and assigns actions, correctly reclassifies `reports/` as CI artifact landing, adds CI artifact rows/tests, makes docs build and pre-commit hard gates, and defines rollback/approval-marker/upstream-export behavior. The remaining blocker is the unresolved contradiction around `outputs/b1528_sirocco` reference policy and its broken verification gate.
