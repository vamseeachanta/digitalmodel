Isolated collection through both conftests succeeds — the RED-capture harness is empirically viable. Review complete.

## Verdict

**MINOR**

## Retrieval

- Read `docs/plans/2026-07-09-issue-701-oracle-dependency-isolation.md` in full (635 lines).
- Read `pyproject.toml` lines 1–80, 145–160, 189, 401–415: confirmed `requires-python = ">=3.11"` (line 10), `cx-Oracle==6.3.1` (line 42), `hiredis==2.2.3` (line 55), `fastapi-limiter` (line 48), `pytest-randomly` (line 96), `pytest-json-report` (line 101); no `oracle` extra; `solvers` extra exists (line 189); `[dependency-groups]` has `dev`/`docs`.
- Read `.github/workflows/workflow-automation-tests.yml` in full: Ubuntu-only matrix, Python 3.11/3.12; push paths include the workflow file but not `pyproject.toml`/`uv.lock`; PR paths omit all three; all five named step commands present; coverage upload gated to Ubuntu/3.11 — every workflow claim matches.
- Read `README.md` lines 80–91: line 90 says "Requires Python 3.10+." as claimed; lxml build caveat at lines 80–84.
- Grepped `uv.lock`: `cx-oracle` (package line 1263, direct edge 1398, requires-dist 1596), `hiredis` (package 2570, edge 1414, requires-dist 1619); hiredis has 2 cp311-win wheels and **zero cp312 wheels** (claim at plan line 89 verified); `redis 7.2.0` locked with no hiredis dependency edge; `fastapi-limiter → redis` edge confirmed; lxml 4.9.3 has full cp312 wheel coverage (manylinux/musllinux/mac/win), so the README-caveat reconciliation in step 6 is sound.
- `git grep`/`rg` for `cx[_-]?oracle|oracledb|hiredis` over `src tests .github config`: only git-ignored `src/digitalmodel.egg-info/` (confirmed ignored via `git check-ignore`) and the dashboard backend `requirements.txt:17-18` — matches plan exactly.
- Verified existence of all 15 plan-referenced paths (`tests/contracts/` present, `test_dependency_isolation.py` absent, three cache test files, both `cache.py` files with `enable_redis: bool = True`, sibling `../assetutilities/src`, `../llm-wiki/scripts/coordination/claim.py`, `HANDOFF_TEMPLATE.md`, workspace-hub plans README and drive-index-search script, `.planning/plan-approved/`); `docs/plans/README.md` absent locally as claimed.
- `git rev-parse HEAD` = `4ab92d90…bc32`, matching the plan's reproduction SHA; host `uv 0.11.25`, matching the pinned lock-regeneration version.
- **Empirical:** `uv run --isolated --python 3.11 --with redis==7.2.0 --with hiredis==2.2.3` → `HIREDIS_AVAILABLE = False` — the plan's load-bearing "pin is installed but inactive" claim is true.
- **Empirical:** re-ran the plan's 93-ref pyproject audit verbatim → `CHECKED=93 WITHOUT_CX=0 WITH_ORACLEDB=0`, identical to plan lines 253–255.
- **Empirical:** isolated `--no-project --with pytest --with pyyaml --with packaging` pytest collection through `tests/conftest.py` and `tests/contracts/conftest.py` succeeds (5 tests collected), so the pre-change RED command's conftest/assetutilities prerequisite chain works on this host; `assetutilities/__init__.py` is stdlib-only with `__version__`.
- Read round-8 artifacts: Claude verdict MINOR, Codex `UNAVAILABLE (rc=126: Argument list too long)`, Gemini `UNAVAILABLE (no non-interactive auth)` — matches the Adversarial Review Summary.
- **Not verifiable from this host:** live GitHub state of #701/#700/#699 (`gh` is not installed here and I have no connected GitHub app), and the drive-file search result. Treated as unverified assertions; nothing in the repo contradicts them.
- Observed `scripts/review/results/issue-701-round-9/` already contains a zero-byte `claude.md`, a zero-byte `claude.md.err`, and a 338-byte `gemini.md`; per the plan's own artifact-timing rule (line 14) these in-run observations are excluded as evidence, but they inform Finding 2.

## Findings

1. **CI cannot continuously enforce the compiler-free deliverable.** The Deliverable section promises "`digitalmodel` installs without an MSVC/VC toolchain on supported Windows Python 3.11/3.12 environments," and the Gaps section frames "Windows clean-install … coverage are absent" as a gap this issue closes. But `windows-latest` GitHub runners ship MSVC Build Tools, so the restored Windows legs (step 5) will silently source-build any *future* sdist-only pin; the only durable guards are name-specific (`find_spec` for `cx_Oracle`/`oracledb`/`hiredis` plus the contract test's three-name denylist). The compiler-free property is proven exactly once, manually, in step 4 and never again. The plan should state this residual explicitly (e.g., in Risks) or add a generic guard (such as `--only-binary`-style install or a wheels-only assertion) rather than implying the CI restoration closes the clean-install coverage gap.

2. **The approval gate's "two usable no-MAJOR plan reviews" may be unsatisfiable, and "usable" is undefined against the mechanical validation.** Round-8 evidence on disk shows Codex died at invocation (`rc=126: Argument list too long`) and Gemini has no non-interactive auth; the plan's own summary table (lines 584–589) acknowledges both, yet the revision requires "a fresh two-provider round" without documenting what changed in the Codex invocation to avoid the identical rc=126 failure, and with Gemini expected to remain unavailable. Separately, the Approval Gate validates artifacts only as "non-empty, newer than this reviewed plan revision, no retained `.err` sibling" — a 338-byte `UNAVAILABLE` stub (the round-8 `gemini.md` is exactly that, and an identically sized file already sits in round-9) passes all three mechanical checks while carrying zero review signal. Define "usable" concretely (e.g., a Verdict line in {APPROVE, MINOR, MAJOR} with a non-empty Retrieval section) and state the fallback if only one usable provider materializes, otherwise the gate blocks indefinitely with no documented exit.

3. **Live-GitHub and drive-search evidence is not reproducible by a reviewer on this host.** Plan lines 183–191 (issue states/labels/comments) and 258–273 (drive search) rest on a connected GitHub app and workspace-hub index infrastructure; `gh` is absent here, so a reviewer cannot independently confirm #701's `status:needs-plan` label or comment count. Everything checkable in-repo corroborated the plan, so this is a verification-boundary note, not a suspected falsehood — but the approval gate's issue-state transitions (`status:plan-review` → `status:plan-approved`) inherit this single-channel dependency and fail closed only if the operator honors the "stop" instructions.

The plan was additionally reviewed against: file-path existence (15/15 verified), line-number drift (none), lock-graph claims (all verified, including the hiredis-inactivity claim tested empirically), the 93-ref drift audit (reproduced exactly), the RED-harness prerequisite chain (exercised empirically, works), wheel-coverage claims for hiredis and lxml (verified from the lock), YAML `on`-key handling under `BaseLoader` (correct), `-p no:randomly`/`--json-report` plugin availability (both in root deps), and internal consistency of the TDD RED/GREEN table against the current workflow file (consistent). No further defects found.

## Blockers

None — all findings are MINOR. Findings 1 and 2 should be addressed in the plan text (a stated residual risk and a concrete "usable review" definition/fallback) before the approval gate is exercised, but neither invalidates the design or the implementation steps.
