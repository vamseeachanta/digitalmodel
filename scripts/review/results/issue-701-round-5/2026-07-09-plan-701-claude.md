## Verdict

MINOR

## Retrieval

- Read `docs/plans/2026-07-09-issue-701-oracle-dependency-isolation.md` in full (484 lines).
- Read `pyproject.toml` in full — confirmed line 10 `requires-python = ">=3.11"`, line 42 `"cx-Oracle==6.3.1"`, line 55 `"hiredis==2.2.3"`, no `redis` root dep (only `fastapi-limiter==0.1.6` at line 48), no oracle extra, `[tool.pytest.ini_options]` addopts/filterwarnings.
- Read `.github/workflows/workflow-automation-tests.yml` in full — confirmed ubuntu-only matrix with `'3.11', '3.12'`, PR paths omit the workflow file + both metadata files, push paths omit `pyproject.toml`/`uv.lock`, the four commands the plan freezes, and the `ubuntu-latest && '3.11'` coverage-upload guard.
- Read `README.md:80-104` — line 90 is exactly `Requires Python 3.10+.`.
- Grepped `cx[_-]?oracle|oracledb` and `hiredis` repo-wide; inspected `uv.lock` lines 1263-1266 (cx-oracle block, sdist only, 0 wheels), 1395-1420 and 1590-1625 (both packages are direct edges in the editable `digitalmodel` deps and `requires-dist`), 2570-2589 (hiredis block: cp311 wheels incl. win32/win_amd64, cp312 count = 0), and the `redis` 7.2.0 block (no hiredis optional-dependency edge — so lock regeneration will actually drop the hiredis package).
- Checked cp311/cp312 `win_amd64` wheel presence in the lock blocks for lxml 4.9.3, statsmodels 0.14.0, pyarrow 14.0.1, scikit-learn 1.3.2, pymssql 2.3.8, h5py 3.10.0, pillow 10.1.0, numpy 1.26.4, scipy, pandas, brotli — all present (geopandas/cryptography use universal/abi3 wheels). The "installs without a C++ compiler on Windows 3.11/3.12 after removing only these two pins" deliverable is consistent with the lock.
- Read `tests/conftest.py` (sys.path injection of `../assetutilities/src`, guarded `import digitalmodel`) and `tests/contracts/conftest.py` (module-level assetutilities version resolution).
- Globbed `tests/contracts/` (exists; `test_dependency_isolation.py` absent), `tests/workflows/workflow_automation/` (9 test files), and confirmed `tests/test_cache.py`, `tests/infrastructure/core/test_cache.py`, `tests/data_systems/test_cache_manager.py` all exist.
- `git show 41289638` — confirmed PR #699's squash commit changed the matrix from `[ubuntu-latest, windows-latest] × ['3.10','3.11']` to `[ubuntu-latest] × ['3.11','3.12']`; `git rev-parse HEAD` = `4ab92d90…` matching the plan's reproduction proof.
- Listed `scripts/review/results/issue-701-round-5/` and read `2026-07-09-plan-701-gemini.md`, `…-claude.md.err`, and the head of `…-codex.md.err`.
- `Test-Path` for `scripts/data/drive-index-search/search.py` (digitalmodel: **False**; workspace-hub: **True**), `docs/plans/README.md` (False, as claimed), `docs/domains/structural/plate_capacity_migration.md` (True) + grep confirming "StiffnerBuckling_Cal … (not migrated)" at line 17.
- Grepped guarded redis imports — `src/digitalmodel/infrastructure/{core,persistence}/cache.py:176` (function-level `import redis`), matching the plan's transitive/guarded claim.
- Globbed `.planning/plan-approved/` — the approval-file convention the plan cites exists (85 files).
- Attempted `gh issue view 701` / `gh pr view 699` — **`gh` is not installed on this host**; live-GitHub claims could not be independently verified.
- Read `D:\ws\assetutilities\src\assetutilities\__init__.py` (5 lines, no third-party imports).

## Findings

1. **The round-5 review sink already exists and contains only failed runs; two of three providers are environmentally broken.** Plan line 12 declares `scripts/review/results/issue-701-round-5/…` the "Current review targets" and §Files to Change says their "absence during provider execution is expected." Actual state: `2026-07-09-plan-701-claude.md` is 0 bytes (`.err`: workspace not trusted — "Ignoring 19 permissions.allow entries … Run Claude Code interactively here once and accept the trust dialog"), `…-codex.md` is 0 bytes (33 KB `.err` from a Codex v0.144.0 session that emitted no review), `…-gemini.md` is an UNAVAILABLE stub (no non-interactive Gemini auth). The Approval Gate's "two usable no-MAJOR plan reviews" is currently unsatisfiable on this host, and the plan neither mentions this state nor requires clearing the stale zero-byte files, which could later be mistaken for completed evidence.

2. **Step 4's disposable Windows environments never run the pytest suite that the restored Windows CI leg will run.** §Implementation Steps step 4 runs only "the existing CLI and package-import workflow commands" in the disposable 3.11/3.12 envs; the workflow-automation pytest suite (workflow line 45) is exercised only in the host's solver-synced Python 3.12 env (§Verification Commands line 384). So the exact job the plan re-enables — base `.[dev]` env pytest on windows-latest, including Python 3.11 — is never executed before CI restoration. Mitigating: the pre-#699 matrix ran this suite on Windows 3.11 successfully, so residual risk is low, but adding the pytest command to the disposable-env script is a one-line amendment that closes it.

3. **The drive-file-search evidence cites a path that does not exist in this repo.** §Embedded verification evidence invokes `scripts/data/drive-index-search/search.py` with a repo-relative path; `Test-Path` in digitalmodel returns False. The script lives at `D:\ws\workspace-hub\scripts\data\drive-index-search\search.py`. As quoted, the evidence command is not replayable from the repo the plan lives in and the citation is misleading about where it ran.

4. **The isolated RED command has an unstated dependency on a sibling `../assetutilities` checkout.** In the `--no-project --isolated` env, `tests/contracts/conftest.py:1-8` falls through to a module-level `import assetutilities`, which succeeds only because `tests/conftest.py:51-53` injects `../assetutilities/src` into `sys.path` when that sibling checkout exists. On any runner without it, all of `tests/contracts/` errors at collection and RED capture fails for the wrong reason. Step 1 and §Verification Commands do not state this precondition.

5. **Live-GitHub and cross-repo evidence is unverifiable in the execution environment.** The "Live GitHub state (connected GitHub app)" block (issue #701 OPEN/labels/comments; #699 MERGED) and the fetched-ref audit (`REMOTE_PYPROJECT_REFS_CHECKED=93` across "all eight ecosystem repositories") cannot be independently checked here: `gh` is not on PATH, and the audit ships summary numbers with no committed script or raw output. Local git does corroborate the #699 squash commit and its ubuntu-only change. Note also that step 8 (open PR) and the repo's CLAUDE.md gate (`gh issue list`) presuppose a working `gh` the current host lacks.

The plan was additionally checked for: wrong line-number citations (none — pyproject 10/42/55 and README 90 exact), false no-consumer claims (none — repo-wide grep matches only docs/lock/review artifacts), lock-edge misstatements (none), BaseLoader/string-matrix errors (none — plan already handles it), hidden hiredis dependents in the lock that would survive removal (none — redis 7.2.0 has no hiredis edge), and non-existent cited files (all exist except finding 3).

## Blockers

1. Finding 1 — the review gate cannot currently produce "two usable no-MAJOR reviews": repair the provider environment (accept the workspace trust dialog for the Claude CLI, configure non-interactive Gemini auth or document a two-provider fallback, get Codex emitting output) and clear/regenerate the stale zero-byte round-5 artifacts before the gate is exercised. This blocks approval/implementation as sequenced by the plan's own Approval Gate, though it is an environment fix, not a plan redesign.

Findings 2-4 are one-line plan amendments (add the pytest suite to the disposable-env script; correct the drive-search path to workspace-hub; state the `../assetutilities` sibling-checkout precondition for the isolated RED run); finding 5 is a documentation/tooling note.
