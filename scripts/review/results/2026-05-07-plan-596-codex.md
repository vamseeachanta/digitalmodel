OpenAI Codex v0.129.0 (research preview)
--------
workdir: /mnt/local-analysis/workspace-hub/digitalmodel
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, /home/vamsee/.codex/memories]
reasoning effort: xhigh
reasoning summaries: none
session id: 019e042f-1918-7713-92aa-fa097a23f7ac
--------
user
# Adversarial Plan Review Request: digitalmodel #596

## Role
You are an independent adversarial reviewer. Do not rubber-stamp. Review the repo-structure refactor plan for digitalmodel before any implementation. Find missing scope boundaries, dangerous migrations, weak verification, CI/docs break risks, approval-gate defects, and repo-ecosystem workflow violations.

## Context
#596 digitalmodel repo-structure normalization. Parent anchors workspace-hub#1962 and #2397. Workflow requires issue -> plan -> adversarial review -> status:plan-review -> user approval -> implementation. No implementation is allowed yet.

The repo is a workspace-hub tier-1 engineering repo. Hard gates: plan all issues, TDD before implementation, adversarial review before status:plan-review, user approval before implementation, transactional closeout.

## Questions to answer
1. Is this plan approval-ready for a first repo-by-repo folder/file structure refactor?
2. Does it sufficiently separate generated artifacts vs durable fixtures/reports vs package source/tests/docs/domain assets?
3. Are TDD and verification gates strong enough to prevent import, routing, docs, CI, and artifact-boundary regressions?
4. Are any proposed file movements too broad, under-specified, or likely to break references?
5. What must be fixed before the plan can move to status:plan-review?

## Required output format
Verdict: APPROVE | MINOR | MAJOR

Findings:
- [severity: critical/high/medium/low] finding title — explanation and concrete required change.

Approval readiness: ready | not ready

## Plan Under Review

# Plan for #596: chore(repo-structure): normalize digitalmodel folder/file structure

> **Status:** draft — awaiting adversarial plan review before `status:plan-review`
> **Complexity:** T3
> **Date:** 2026-05-07
> **Issue:** https://github.com/vamseeachanta/digitalmodel/issues/596
> **Review artifacts:** pending — `scripts/review/results/2026-05-07-plan-596-claude.md` | `scripts/review/results/2026-05-07-plan-596-codex.md` | `scripts/review/results/2026-05-07-plan-596-gemini.md`
> **Parent anchors:** workspace-hub#1962, workspace-hub#2397

---

## Resource Intelligence Summary

### Existing repo code and structure

- `digitalmodel#596` — planning issue exists and is open with labels `enhancement`, `cat:engineering`, `priority:high`, `status:pending`. The issue explicitly authorizes planning only; it states implementation requires resource intelligence, plan, adversarial review, `status:plan-review`, user approval, and then TDD execution.
- `AGENTS.md` — identifies `digitalmodel` as an engineering digital-twin repo with source rooted at `src/digitalmodel/`, test command `PYTHONPATH=src uv run python -m pytest`, and dependency on `assetutilities`.
- `docs/README.md` — defines the authoritative routing surfaces and repo-vs-bulk-artifact-store rule: keep source/tests/small curated docs/routing registries in repo; move large/generated/binary/cache/raw/fast-growing artifacts to bulk artifact store.
- `docs/maps/digitalmodel-operator-map.md` and `docs/registry/module-routing.yaml` — provide module-level source/test/docs routing. These are the first structural references that must not be broken by file moves.
- `.gitignore` — already ignores many generated or transient roots (`build/`, `dist/`, `logs/`, `/site`, coverage files, pytest/mypy caches, temp root files), but tracked files can still exist in ignored paths and must be handled with explicit `git rm --cached`/migration decisions after approval.
- `.github/workflows/*.yml` — CI currently creates or references `coverage.xml`, `coverage.json`, `dist/`, `reports/quality_gates_results.json`, and `reports/quality-gates-pytest-full.log`. The plan must not blindly delete/report-move paths until CI upload contracts are updated.

### Standards / workflow contracts

| Contract | Status | Source |
|---|---:|---|
| Issue → plan → adversarial review → `status:plan-review` → user approval → implement | required | workspace-hub `AGENTS.md`; `coordination/issue-planning-mode` |
| TDD before implementation | required | workspace-hub `AGENTS.md`; `issue-planning-mode` |
| Repo root cleanliness and no `tests/` under `src/` | applicable | `workspace-hub/repo-structure` |
| Generated artifacts are not source unless deliberately curated fixtures/reports | applicable | `docs/README.md` repo-vs-bulk-artifact-store rule; `repo-cleanup` |
| Domain/package moves require API compatibility and import/reference scans | applicable | `discipline-refactor`; `repo-structure` |

### Documents consulted

- `workspace-hub/docs/reports/2026-05-07-tier1-folder-structure-inventory.md` — recommends `digitalmodel` first because it is clean, on `main`, and has canonical top-level paths. It identifies drift candidates: `benchmark_output`, `build`, `cache`, `dist`, `logs`, `memory`, `outputs`, `results`, `site`, `test_output_ss`, and root coverage files.
- `digitalmodel/docs/README.md` — provides the repo-specific artifact-store boundary and curated routing surfaces.
- `digitalmodel/docs/maps/digitalmodel-operator-map.md` — confirms module routing spans `src/digitalmodel/*`, `tests/*`, and `docs/domains/*`.
- `digitalmodel/docs/registry/module-routing.yaml` — confirms machine-readable routing entries that must remain aligned after any source/test/docs moves.
- `digitalmodel/docs/plans/2026-05-05-issue-483-curves-py-decomposition.md` — prior plan already found a stale issue pointing at `build/lib/...` rather than tracked source; this is direct evidence that tracked/ignored artifact state can mislead issue planning if not audited first.
- `digitalmodel/docs/plans/2026-05-05-issue-512-gtm-demo-2-from-cache-hermetic.md` — prior plan found tests depending on pre-existing generated HTML reports; this is direct evidence that artifact cleanup must separate durable fixtures from generated outputs.
- `digitalmodel/docs/plans/2026-05-05-issue-469-ai-agent-session-artifact-policy.md` — prior plan addresses AI-agent session artifacts (`.codex`, `.gemini`, `.claude/skills`) and should be treated as a related but separate surface, not silently absorbed here.

### Gaps identified

- No repo-local structure contract file exists yet that classifies top-level `digitalmodel` paths into source, tests, docs, durable fixtures, generated artifacts, caches, reports, notebooks, examples, and domain assets.
- No automated repo-structure check currently asserts that ignored generated roots do not remain tracked.
- No migration matrix currently says whether each candidate path should be deleted, `git rm --cached`, moved to `docs/`, moved to `examples/`, moved to `tests/fixtures/`, archived to bulk artifact store, or left as-is.
- `docs/plans/README.md` is missing in `digitalmodel`; do not invent a broad planning-index convention as part of implementation unless the user explicitly approves it or a separate repo-planning-contract issue is filed.

### Evidence (embedded verification)

**Issue status** (verified 2026-05-07T20:39:06Z via `gh issue view 596`):

```text
#596 — OPEN — chore(repo-structure): normalize digitalmodel folder/file structure
labels: enhancement, cat:engineering, priority:high, status:pending
```

**Working tree / branch proof** (verified 2026-05-07T20:39:06Z):

```text
$ git status --short --branch
## main...origin/main
```

**Tracked top-level distribution** (verified 2026-05-07T20:39:06Z via `git ls-files | awk -F/ '{print $1}' | sort | uniq -c | sort -nr | sed -n '1,80p'`):

```text
8687 docs
2206 src
1552 tests
590 scripts
563 .claude
272 examples
220 config
83 .planning
18 outputs
15 assets
14 .github
3 data
1 notebooks
1 .gemini
1 .codex
... root control/config files ...
```

**Tracked root files** (verified 2026-05-07T20:39:06Z):

```text
AGENTS.md
CHANGELOG.md
CLAUDE.md
_count_tests.py
_coverage_map.py
.coveragerc
.editorconfig
.env.template
.gitattributes
.gitignore
.gitmodules
LICENSE
.license-header.txt
Makefile
mkdocs.yml
.mutmut_config
orcaflex_maturity_analysis.py
.pre-commit-config.yaml
pyproject.toml
pytest.ini
.radon.cfg
README.md
ROADMAP.md
uv.lock
uv.toml
vulture_whitelist.py
```

**Tracked generated/artifact candidates** (verified 2026-05-07T20:39:06Z):

```text
_count_tests.py
_coverage_map.py
outputs/b1528_sirocco/b1528_sirocco_yaw_moment_manifest.json
outputs/b1528_sirocco/b1528_sirocco_yaw_moment_provenance.json
outputs/b1528_sirocco/b1528_sirocco_yaw_moment_report.html
outputs/b1528_sirocco/b1528_sirocco_yaw_moment_report.md
outputs/b1528_sirocco/b1528_sirocco_yaw_moment_results.csv
outputs/b1528_sirocco/b1528_sirocco_yaw_moment_results.json
outputs/b1528_sirocco/moored_current/... report/result/provenance files
outputs/b1528_sirocco/time_trace/... report/result/provenance files
```

**Canonical path existence** (verified 2026-05-07T20:39:06Z):

```text
EXISTS .claude
EXISTS .github
EXISTS config
EXISTS docs
EXISTS scripts
EXISTS src
EXISTS tests
EXISTS benchmarks
EXISTS memory
EXISTS notebooks
EXISTS projects
EXISTS reports
EXISTS specs
```

**CI artifact references** (verified 2026-05-07T20:39:06Z from `.github/workflows/*.yml`):

```text
coverage.xml is uploaded by domain test workflows.
quality-gates.yml uploads reports/quality_gates_results.json, reports/quality-gates-pytest-full.log, coverage.json, coverage.xml.
orcaflex-tests.yml builds distribution with python -m build and uploads dist/.
docs.yml runs uv run --group docs mkdocs build --strict.
```

**Reproduction proofs** (verify-against-repo-state):

N/A — this is a governance/structure-planning issue. It does not allege a runtime failure, broken import, missing method, incorrect numeric output, or regression. Runtime checks are listed as implementation verification gates below.

---

## Artifact Map

| Artifact | Path |
|---|---|
| This plan | `docs/plans/2026-05-07-issue-596-repo-structure-normalization.md` |
| Structure contract to create/extend | `docs/standards/repo-structure.md` or `docs/README.md` section (final path to be confirmed during implementation) |
| Machine-readable allowlist/classification | `config/repo_structure.yml` or `config/quality/repo_structure.yml` (choose one during implementation; do not duplicate) |
| Repo-structure verification script | `scripts/verify-repo-structure.py` or `scripts/maintenance/verify_repo_structure.py` |
| TDD tests for structure checker | `tests/repo_structure/test_repo_structure_contract.py` |
| Optional docs/routing tests to extend | `tests/docs/test_digitalmodel_routing_contract.py` if existing; otherwise create targeted tests under `tests/docs/` |
| Potential tracked artifact removals/moves | `_count_tests.py`, `_coverage_map.py`, `outputs/b1528_sirocco/**` after classification approval |
| Generated-path ignore updates | `.gitignore` |
| CI path updates if report/coverage behavior changes | `.github/workflows/*.yml` |
| Plan review — Claude | `scripts/review/results/2026-05-07-plan-596-claude.md` |
| Plan review — Codex | `scripts/review/results/2026-05-07-plan-596-codex.md` |
| Plan review — Gemini | `scripts/review/results/2026-05-07-plan-596-gemini.md` |

---

## Deliverable

A conservative `digitalmodel` repo-structure normalization that first installs a documented, tested artifact-boundary contract, then applies only approved low-risk moves/removals for generated artifacts/root utilities while preserving package imports, docs routing, CI artifact upload behavior, and durable engineering evidence.

---

## Path-Level Migration Matrix

| Path / pattern | Current classification | First-pass action after approval | Breaking risk | Verification |
|---|---|---|---:|---|
| `src/digitalmodel/**` | package source | Do not move in first pass | high | import smoke tests; module-routing alignment |
| `tests/**` | tests | Do not move in first pass except tests created for checker | high | `uv run pytest tests/repo_structure -v`; representative smoke tests |
| `docs/**` | docs/routing/plans/domain docs | Do not bulk-move; add structure contract only | medium | docs link/path search; `uv run --group docs mkdocs build --strict` if deps available |
| `config/**` | config | Do not move; may add structure checker config | low | config parser test |
| `scripts/**` | automation | Add checker here if selected; no unrelated moves | medium | script unit tests; direct checker run |
| `.github/workflows/**` | CI | Modify only if generated-report locations change | high | inspect workflow references; no stale paths |
| `outputs/b1528_sirocco/**` | tracked generated/demo report evidence candidate | Classify as durable fixture vs generated output before moving. If durable, move under `docs/examples/` or `tests/fixtures/` with references; if generated, remove from git and document regeneration command/artifact store | medium | reference search; fixture/report test; `git ls-files outputs/**` empty or justified |
| `_count_tests.py`, `_coverage_map.py` | root utility scripts | Move to `scripts/maintenance/` if still used; otherwise remove with reference proof | medium | reference search; direct invocation or deletion proof |
| `benchmark_output/`, `build/`, `dist/`, `cache/`, `logs/`, `results/`, `site/`, `test_output_ss/` | generated/ignored roots from inventory | Verify whether tracked; if untracked, leave deletion to cleanup command and ensure `.gitignore`; if tracked, untrack/remove after approval | low/medium | `git ls-files <path>`; `.gitignore` tests |
| `.benchmarks/`, `.hypothesis/`, `.mypy_cache/`, `.pytest_cache/`, `.ruff_cache/`, `.swarm/` | local developer/runtime state | Ensure ignored; do not track; cleanup local-only after approval if requested | low | `git status --ignored --short`; `git ls-files` |
| `benchmarks/`, `memory/`, `notebooks/`, `projects/`, `reports/`, `specs/` | domain/product assets requiring classification | Do not move in first pass. Add classification contract and open follow-up issues for any controversial migration | high | path inventory and reference search |
| `.codex`, `.gemini`, `.claude` | agent/tooling surfaces | Do not alter beyond respecting issue #469; avoid duplicating AI-session policy | medium | confirm #469 state before any future work |

---

## Pseudocode

```text
function classify_path(path):
    load repo_structure.yml allowlist and generated-pattern list
    if path under src/ or tests/ or docs/domain routing: return durable_source_or_test_or_doc
    if path matches known build/cache/report output pattern: return generated_candidate
    if path is root utility: search references and classify used_script vs stale_script
    if path under domain asset dirs: return domain_asset_needs_followup
    return unknown_needs_manual_review

function verify_repo_structure(repo_root):
    tracked = git ls-files
    ignored = parse .gitignore-sensitive patterns
    for each tracked path:
        category = classify_path(path)
        emit finding if generated_candidate is tracked without allowlist justification
        emit finding if root file is not in root_allowlist
        emit finding if src/**/tests exists
    validate docs/README curated routing paths exist
    validate module-routing.yaml source/test/docs paths exist or are explicitly optional
    return nonzero only for violations introduced after allowlist baseline is defined

function migrate_first_pass():
    run verifier in baseline mode and commit classification docs/config/tests first
    for each approved low-risk path action:
        search references
        perform move/remove/untrack
        update references and .gitignore
        run verifier and targeted tests
    stop before domain-asset/package-source migrations
```

---

## Files to Change

| Action | Path | Reason |
|---|---|---|
| Create | `docs/plans/2026-05-07-issue-596-repo-structure-normalization.md` | canonical plan artifact |
| Create/modify | `docs/standards/repo-structure.md` or `docs/README.md` | durable repo-specific structure contract |
| Create | `config/repo_structure.yml` or `config/quality/repo_structure.yml` | machine-readable root/path allowlist and generated-artifact rules |
| Create | `scripts/verify-repo-structure.py` or `scripts/maintenance/verify_repo_structure.py` | repeatable verification instead of ad hoc cleanup |
| Create | `tests/repo_structure/test_repo_structure_contract.py` | TDD guardrail for structure checker |
| Modify | `.gitignore` | align generated/cache/report patterns with contract |
| Modify | `.github/workflows/*.yml` | only if report/coverage output paths are changed |
| Move/remove/untrack | `_count_tests.py`, `_coverage_map.py`, `outputs/b1528_sirocco/**` | only after reference search and classification prove safe |
| No-op first pass | `src/digitalmodel/**`, `tests/**`, domain asset dirs (`benchmarks/`, `memory/`, `notebooks/`, `projects/`, `reports/`, `specs/`) | avoid breaking API/domain routing in first repo-structure pass |

---

## TDD Test List

| Test name | What it verifies | Expected input | Expected output |
|---|---|---|---|
| `test_root_allowlist_accepts_current_control_files` | root control/config files are explicitly allowed | current root file list | no violations for approved root files |
| `test_generated_artifacts_are_not_tracked_unless_allowlisted` | tracked `build/dist/site/logs/cache/results/coverage` candidates fail unless allowlisted | synthetic tracked path list | violation emitted |
| `test_outputs_require_durable_classification` | `outputs/**` cannot remain tracked without fixture/report justification | `outputs/b1528_sirocco/...` | durable classification required |
| `test_src_tests_nested_paths_rejected` | prevents `tests/` inside `src/` | `src/digitalmodel/foo/tests/test_x.py` | violation emitted |
| `test_module_routing_paths_exist` | routing registry paths still point to existing source/test/doc paths | `docs/registry/module-routing.yaml` | all required paths exist or optional paths marked |
| `test_docs_readme_curated_surfaces_exist` | docs entrypoint does not reference missing routing files | `docs/README.md` | all curated surfaces exist |
| `test_checker_cli_returns_nonzero_for_bad_fixture` | checker can be used in CI/pre-commit later | temporary fixture repo | nonzero with clear message |
| `test_checker_cli_returns_zero_for_clean_fixture` | checker has a passing baseline | temporary fixture repo | zero exit |

---

## Verification / Acceptance Criteria

Plan-stage criteria:

- [x] Issue body read and status verified.
- [x] `AGENTS.md`, `docs/README.md`, operator map, module-routing registry, `.gitignore`, CI workflows, and prior related plans consulted.
- [x] Plan separates generated artifacts, durable reports/fixtures, docs, scripts/tools, package source, tests, and domain assets.
- [x] Plan identifies safe housekeeping vs breaking path migrations.
- [x] Plan explicitly states what will not be moved in the first implementation pass.
- [ ] Adversarial plan reviews exist under `scripts/review/results/`.
- [ ] Issue moved to `status:plan-review` only after review artifacts exist.
- [ ] User approval marker exists before implementation: `.planning/plan-approved/596.md`.

Implementation criteria after user approval:

- [ ] Write structure-checker tests first and confirm RED before implementation.
- [ ] Implement checker/config/docs until TDD tests pass: `PYTHONPATH=src uv run python -m pytest tests/repo_structure -v`.
- [ ] Run structure checker against current repo and capture output.
- [ ] Run routing/docs tests, at minimum `PYTHONPATH=src uv run python -m pytest tests/docs -v` if present.
- [ ] Run representative smoke tests that do not require licensed solvers: import/package smoke, repo-structure tests, and selected domain tests agreed in review.
- [ ] Run docs build if environment supports it: `uv run --group docs mkdocs build --strict`; if unavailable, document blocker and run link/path scans.
- [ ] Reference search proves no stale references to moved/removed paths.
- [ ] `git ls-files` proves generated roots are either untracked or explicitly allowlisted as durable fixtures.
- [ ] Commit, push, issue comment, closeout, branch/worktree cleanup, and clean-state proof occur transactionally in one closeout window.

---

## Adversarial Review Summary

Pending. This draft must be sent for adversarial review before it is surfaced as approval-ready or labeled `status:plan-review`.

| Provider | Verdict | Key findings |
|---|---|---|
| Claude | PENDING | not yet run |
| Codex | PENDING | not yet run |
| Gemini | PENDING | not yet run |

**Overall result:** PENDING — draft only, not approval-ready.

---

## Risks and Open Questions

- **Risk — tracked outputs may be durable examples.** `outputs/b1528_sirocco/**` may be demo/report evidence rather than disposable generated output. Do not delete until reference search and owner-intent classification prove it is disposable or a replacement durable location is chosen.
- **Risk — CI artifact paths.** Workflows upload `coverage.xml`, `coverage.json`, `reports/**`, and `dist/`. Cleaning root artifacts without updating CI could break evidence upload or create new root clutter.
- **Risk — broad domain asset dirs.** `benchmarks/`, `memory/`, `notebooks/`, `projects/`, `reports/`, and `specs/` may carry domain knowledge. First pass should classify and document, not bulk-migrate.
- **Risk — AI-agent artifact overlap.** `.codex`, `.gemini`, and `.claude` overlap with #469; keep this issue focused on repo structure and do not silently rewrite AI-session policy.
- **Open — checker path.** Choose one of `scripts/verify-repo-structure.py` or `scripts/maintenance/verify_repo_structure.py` during implementation; avoid duplicate tooling.
- **Open — config path.** Choose one of `config/repo_structure.yml` or `config/quality/repo_structure.yml`; prefer existing config organization if discovered during implementation.
- **Open — plan index.** `digitalmodel/docs/plans/README.md` is absent. This plan does not create one; if a plan index is desired, file a separate repo-planning-contract issue or explicitly approve it in this issue.

---

## Approval Gate

Implementation is blocked until all are true:

1. Adversarial review artifacts are written under `scripts/review/results/` and this plan is revised if any MAJOR/MINOR finding requires changes.
2. The issue is labeled `status:plan-review` and the user explicitly approves the reviewed plan.
3. The issue is labeled `status:plan-approved` and local marker `.planning/plan-approved/596.md` exists.
4. Executor begins with TDD tests and does not perform bulk moves outside the approved matrix.

---

## Complexity: T3

**T3** — repo-structure normalization crosses root policy, docs/routing contracts, CI artifact handling, generated-vs-durable evidence classification, and future repo-by-repo ecosystem reuse. It must be planned/reviewed carefully before implementation.


warning: Codex's Linux sandbox uses bubblewrap and needs access to create user namespaces.
codex
I’ll use the Superpowers startup guidance first, then I’ll keep this as a plan review only and avoid any implementation actions.
codex
The skill file read was blocked by the local sandbox wrapper (`bwrap` loopback error). I’ll proceed from the visible skill trigger rules and keep the response constrained to the requested adversarial plan review.
codex
Verdict: MAJOR

Findings:
- [severity: high] Migration scope is still under-specified — `outputs/b1528_sirocco/**`, `_count_tests.py`, and `_coverage_map.py` are listed for move/remove/untrack, but final destination, retention policy, owner intent, and regeneration evidence are deferred to implementation. Required change: split the plan into Phase 1 contract/checker only, and a separate approved migration matrix with one row per concrete path action before any file movement.

- [severity: high] Durable artifact boundary is not approval-grade yet — the plan recognizes generated-vs-durable ambiguity, but does not define objective classification rules for reports, fixtures, examples, provenance files, demo outputs, or domain evidence. Required change: add explicit criteria and required evidence for each category, including where durable reports live, how fixtures are named, and when artifact-store migration is required.

- [severity: high] Verification gates are too generic for CI and routing risk — “representative smoke tests” and “selected domain tests agreed in review” are not concrete enough. Required change: name the exact smoke test commands, exact routing/doc tests, and exact CI workflow contracts that must be checked after each approved move.

- [severity: medium] CI artifact handling is identified but not gated transactionally — workflows write/upload `coverage.xml`, `coverage.json`, `reports/**`, and `dist/`, but the plan does not require proving these are runtime products rather than tracked repo artifacts after cleanup. Required change: add a CI artifact contract test or scripted check that distinguishes allowed generated CI outputs from tracked generated files.

- [severity: medium] Checker design could freeze the current mess as policy — the plan says baseline mode and allowlists, but does not define expiry, justification fields, or fail behavior for grandfathered violations. Required change: require every allowlisted generated/durable exception to include owner, reason, category, and follow-up issue or explicit permanent justification.

- [severity: medium] Approval gate can be bypassed by local marker ambiguity — `.planning/plan-approved/596.md` is required, but the plan does not define its required contents or source of truth. Required change: marker must cite the exact user approval comment/issue URL, timestamp, reviewed plan SHA/path, and approved migration matrix; local marker alone must not authorize implementation.

- [severity: medium] Root-script migration remains risky — moving/removing `_count_tests.py` and `_coverage_map.py` can break ad hoc developer workflows, docs, Makefile targets, or CI. Required change: require `rg` reference proof plus direct execution or explicit deprecation/removal note before any action.

- [severity: low] Tool/config path choices are still open — choosing `scripts/verify-repo-structure.py` vs `scripts/maintenance/verify_repo_structure.py` and `config/repo_structure.yml` vs `config/quality/repo_structure.yml` during implementation creates avoidable review drift. Required change: decide these paths in the reviewed plan.

Approval readiness: not ready
tokens used
31,552
Verdict: MAJOR

Findings:
- [severity: high] Migration scope is still under-specified — `outputs/b1528_sirocco/**`, `_count_tests.py`, and `_coverage_map.py` are listed for move/remove/untrack, but final destination, retention policy, owner intent, and regeneration evidence are deferred to implementation. Required change: split the plan into Phase 1 contract/checker only, and a separate approved migration matrix with one row per concrete path action before any file movement.

- [severity: high] Durable artifact boundary is not approval-grade yet — the plan recognizes generated-vs-durable ambiguity, but does not define objective classification rules for reports, fixtures, examples, provenance files, demo outputs, or domain evidence. Required change: add explicit criteria and required evidence for each category, including where durable reports live, how fixtures are named, and when artifact-store migration is required.

- [severity: high] Verification gates are too generic for CI and routing risk — “representative smoke tests” and “selected domain tests agreed in review” are not concrete enough. Required change: name the exact smoke test commands, exact routing/doc tests, and exact CI workflow contracts that must be checked after each approved move.

- [severity: medium] CI artifact handling is identified but not gated transactionally — workflows write/upload `coverage.xml`, `coverage.json`, `reports/**`, and `dist/`, but the plan does not require proving these are runtime products rather than tracked repo artifacts after cleanup. Required change: add a CI artifact contract test or scripted check that distinguishes allowed generated CI outputs from tracked generated files.

- [severity: medium] Checker design could freeze the current mess as policy — the plan says baseline mode and allowlists, but does not define expiry, justification fields, or fail behavior for grandfathered violations. Required change: require every allowlisted generated/durable exception to include owner, reason, category, and follow-up issue or explicit permanent justification.

- [severity: medium] Approval gate can be bypassed by local marker ambiguity — `.planning/plan-approved/596.md` is required, but the plan does not define its required contents or source of truth. Required change: marker must cite the exact user approval comment/issue URL, timestamp, reviewed plan SHA/path, and approved migration matrix; local marker alone must not authorize implementation.

- [severity: medium] Root-script migration remains risky — moving/removing `_count_tests.py` and `_coverage_map.py` can break ad hoc developer workflows, docs, Makefile targets, or CI. Required change: require `rg` reference proof plus direct execution or explicit deprecation/removal note before any action.

- [severity: low] Tool/config path choices are still open — choosing `scripts/verify-repo-structure.py` vs `scripts/maintenance/verify_repo_structure.py` and `config/repo_structure.yml` vs `config/quality/repo_structure.yml` during implementation creates avoidable review drift. Required change: decide these paths in the reviewed plan.

Approval readiness: not ready
