## Verdict: **MAJOR** — composition now binds real artifacts; the log side of the binding is not the artifact it is labelled as

Checked: `tests/ansys/padeye_pressure_bundle.py`, `test_padeye_pressure_bundle.py`, `padeye_pressure_quality.py`, the three new fixtures, `.gitattributes:35-36`, and re-walked `padeye_pressure_cdb.py` / `padeye_pressure_study.py` against the committed CDB bytes.

**Verified against the actual fixture (r1 items closed):** NBLOCK 1110 rows (`pressure-coarse-v261.cdb:23-1132`) and EBLOCK 1032 rows (`:1136-2167`) match `INPUT_MATRIX` (`padeye_pressure_bundle.py:16`); SFEBLOCK header `1032,256,0` (`:2282`) satisfies `read_pressures` (`padeye_pressure_cdb.py:120-122`), 32 rows → 16 faces = `upper_edges`; rows are exactly 97 columns (9+4+4+4×20), so the 4-value read passes `fields`' trailing-field guard despite the `6(pg20.9)` descriptor — r1 #4 resolved against the artifact. `read_nodes`' `require(values[3] …)` (`:68`) is consistent with the observed export: node 193 at the origin writes X explicitly (`:215`) and omits only trailing Y/Z. Pressure rows are symmetric about element 8/9 at 100.113885 MPa (`:2298-2300`), matching `alpha·gradient·(y−220)` with `gradient = 2·50000/(π·40²·8)`. r1 #2 and #3 are closed by `test_padeye_pressure_bundle.py:57-63`; r1 #5 is resolved by the single-summary fixture.

### Blocking

**1. `log_sha256` is a digest of a reconstructed file, and the bundle reports it as a verified capture artifact.** `pressure-coarse-v261-provenance.json:8` states the excerpt was "reproduced as explicit format fixture," and the file corroborates it: `pressure-coarse-shape-v261.txt:21` has a leading space, `:22` does not — Ansys does not emit that pair inconsistently. `.gitattributes:36` further marks it `text eol=lf` while the CDB is `-text` (`:35`), so the log digest is over normalized bytes and cannot equal any Windows-captured log. Consequence: `assess_shape_output` returns `warning_count: 0, error_count: 0` (`padeye_pressure_quality.py:31`) as a property of an authored file, and `verify_pressure_bundle` folds it into `artifact_hashes_verified: 3` (`padeye_pressure_bundle.py:42`) with no key distinguishing verbatim capture from format reproduction. `raw_log_sha256` (`provenance:6`) is consumed by nothing — repo-wide it appears only in that file. That is r1's root issue relocated one level up: the CDB side is now real bytes, the log side is still prose. Fix: split the return contract (`log_artifact_class: 'format_reproduction'`, or a distinct `shape_excerpt_sha256` key), and either commit the raw log or drop `raw_log_sha256` rather than carry an unverifiable field the reporter may cite.

**2. No negative test exercises the gate arms of the composition — only the hash arm.** `test_altered_input_database_or_log_rejected` (`:41-46`) appends `b'\n'` to each artifact, so all three indices are rejected at `padeye_pressure_bundle.py:29` before `parse_pressure_cdb`, `verify_frozen_pressure_domain`, `verify_pressure_mesh` or `assess_shape_output` runs. `test_self_rehashed_changed_generator_is_not_the_frozen_case` (`:49-54`) supplies the re-hashed analogue for index 0 only, and even that is refused by the matrix lookup (`:33`), not by a gate. The receipt is caller-attested by design (module docstring `:3-5`), so hash equality proves only the caller's claim; the gates are the entire defence and none of their rejection paths are reached from this module's tests. Add the index-1/index-2 analogue: perturb one SFEBLOCK value or the `Element count 1032` line, recompute the corresponding digest into `expected`, assert `ValueError`.

### Minor

- `assess_shape_output` still has no type guard on `text` (r1 minor, `padeye_pressure_quality.py:9`): non-`str` raises `AttributeError`, outside both the module's `ValueError` contract and the bundle's `except (KeyError, TypeError, UnicodeError)` (`padeye_pressure_bundle.py:47`). Guard `text` alongside `expected_elements` at `:7`.
- `assert result['pressure']['force_n'][1] == approx(50000)` (`test_padeye_pressure_bundle.py:37`, repeated `:63`) carries no information: `alpha` is solved from the same geometry at `padeye_pressure_study.py:201` so the re-integration is normalized to 50000 by construction once `_load_values` accepts. The real independent content is the face/arc binding (`padeye_pressure_study.py:175`, `:131`). Assert the binding explicitly, or the assertion will be read as force verification in the reporter slice.
- `verify_pressure_bundle` returns no digests or capture ID (`:42-46`), so a downstream reporter must re-derive them from the fixture to cite anything; `artifact_hashes_verified: 3` is a count, not evidence.
- `expected` is not checked for unexpected keys; a receipt carrying additional attestations is silently ignored.
- `provenance:4` records capture `20260913T021534Z`, dated after today. Benign if the capture host is behind UTC; state the offset in the provenance record rather than leaving a future timestamp in the evidence chain.