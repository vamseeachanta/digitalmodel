# Issue 1602 Plan Review — Round 4 Solver/Engineering

## Verdict

MAJOR

## Retrieval

Reviewed pushed commit `825c391edad7aa06832063a1eba6b9154093196e`, the parent plan, and all four normative files. Structural and inline semantic checks passed.

## Findings

1. One mandatory pipe-property shape cannot represent tensioners, LMRP, BOP, tree, or lower package without fabricated values.
2. Top/lower boundary attachments, end/orientation/stiffness, tensioner geometry, nonlinear behavior, and connector stiffness remain incomplete.
3. Seawater/gravity/seabed, wave period/phase, normalized RAO/time-history, and external-pressure inputs remain insufficient.
4. Result row IDs do not explicitly exclude `release_id`; staging is owned by `execution_id` before model hash makes that ID constructible.
5. Canary claims lack a trusted expected result/tolerance, complete signed schema, and independent verification.
6. `verified_success` has no normative OrcaFlex semantic derivation.
7. The common quantity list applies line quantities to rigid-package locations and omits LMRP/BOP semantics.
8. Oracle applicability predicates removed in Draft 4 remain unresolved.
9. Inline semantic validation checks selected presence/order only, not the declared semantic contract.
10. Zero surface current with nonzero subsurface current is physically valid but rejected.
11. Released lineage `entity_id` has no derived seed/final binding rule.

## Blockers

Use discriminated physical variants and explicit boundaries; complete environment inputs; introduce a provisional request/stage identity; define trusted canary and semantic success; freeze a quantity/location matrix and oracle predicates; and execute only semantic claims the validator actually implements.
