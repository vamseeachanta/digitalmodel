## Verdict

MAJOR

## Retrieval

- Read `docs/plans/2026-07-09-issue-701-oracle-dependency-isolation.md` in full (325 lines).
- Read `pyproject.toml` in full — confirmed `requires-python = ">=3.11"` (line 10), `cx-Oracle==6.3.1` (line 42), `hiredis==2.2.3` (line 55), no `redis` package anywhere in root dependencies or extras, `workflow-automation` script entry (line 218).
- Read `.github/workflows/workflow-automation-tests.yml` in full — confirmed Ubuntu-only matrix with Python 3.11/3.12, PR paths omitting the workflow file + both metadata files, push paths omitting the metadata files, coverage upload gated to ubuntu/3.11, and the four exact commands the plan promises to preserve.
- Grep (case-insensitive) `cx[_-]?oracle|oracledb` repo-wide — hits only in `pyproject.toml`, `uv.lock`, the plan itself, review artifacts, and the archived `docs/domains/platecapacity/StiffnerBuckling_Cal/Python_Environment/` tree + one article. No `src/`, `tests/`, `.github/`, or `config/` consumer — plan claim confirmed.
- Grep `uv.lock` — `cx-oracle` package (line 1263), direct edges from editable `digitalmodel` (lines 1398, 1596), `hiredis` edges (lines 1414, 1619), hiredis 2.2.3 wheel list (lines 2570–2589): cp311 Windows wheels **exist**, no cp312 wheels exist. Also confirmed lxml 4.9.3, statsmodels 0.14.0, psutil 5.9.6 all carry cp312/abi3 Windows wheels in the lock (no obvious third blocker).
- Grep `\bhiredis\b` in `src/` — only `visualization/orcaflex_dashboard/backend/requirements.txt` (a separate docker env). Grep `import redis` in `src/digitalmodel/infrastructure/` — lazy, guarded import inside `_init_redis()` at `cache.py:176`, opt-in via `config.enable_redis`.
- Read `README.md` lines 25–64 and grep'd for "3.10" — the "Python 3.10+" drift is real but at `README.md:90`, not line 38.
- Grep `docs/domains/structural/plate_capacity_migration.md` — lines 16–17 confirm `StiffnerBuckling_Cal/` "(not migrated)".
- Listed `scripts/review/results/*701*` — unsuffixed claude/codex `.md` files are **0 bytes** with `.err` siblings (failed runs, 16:11); round-2 files have content and are **older** (15:53).
- Glob `tests/contracts/**` (exists, no `test_dependency_isolation.py` yet), `tests/workflows/workflow_automation/**` (exists), `src/digitalmodel/__main__.py` (exists), `.planning/plan-approved/*` (convention confirmed, ~85 files), `config/*` (exists).
- `gh` CLI is not installed on this host; verified issue #701 via web fetch instead — open, `cat:engineering`/`lane:claude`/`status:needs-plan`, body records the 2026-07-09 removal-only decision verbatim.
- Fetched `pypi.org/project/hiredis/` — latest is 3.4.0 (2026-06-03) with cp311 and cp312 Windows wheels. Plan claim confirmed.

## Findings

1. **hiredis is retained and contract-frozen despite failing the plan's own removal criterion.** The plan removes cx-Oracle because "no active consumer exists," but hiredis is equally unconsumed at root: nothing in `src/` imports `hiredis` (it is only a C accelerator consumed *through* redis-py), and `redis` itself is **not** a root dependency — the only `import redis` is a guarded, opt-in lazy import at `src/digitalmodel/infrastructure/persistence/cache.py:176` that cannot currently be satisfied by a locked install anyway. Instead of removing this dead pin, the plan upgrades it (`>=3.4.0,<4.0.0`) and adds contract tests (`test_hiredis_uses_windows_wheel_range`, `test_lock_resolves_supported_hiredis`, pseudocode "assert its hiredis direct edge remains") that make its presence a permanent, test-enforced contract. The plan nowhere justifies keep-vs-remove; removal would satisfy the same Windows-install deliverable with strictly less churn and no dead dependency. This is the same class of defect (#701's "unused driver") being entrenched rather than fixed.
2. **Citation error:** Resource Intelligence claims "`README.md:38` still says Python 3.10+". The actual text is at `README.md:90` ("Requires Python 3.10+"); line 38 is a mermaid diagram. The substance survives but the citation is false as written.
3. **The hiredis "next deterministic Windows blocker" claim is 3.12-only, and the plan doesn't say so.** `uv.lock:2588-2589` shows hiredis 2.2.3 ships cp311 win32/win_amd64 wheels; only cp312 wheels are absent. The Reproduction proofs section tested only `--python 3.12`. The Gaps section ("must be raised … for the Windows CI restoration to be real") overstates the blocker as matrix-wide when it blocks only the Python 3.12 leg.
4. **TDD RED count is internally inconsistent.** The plan states "The first six relevant assertions must demonstrate RED" (below the TDD table), but the table itself lists **seven** currently-RED tests: rows 1–6 plus `test_workflow_proves_base_oracle_isolation` ("isolation step absent"). Either the RED-evidence gate under-specifies by one test or the table is wrong.
5. **Review-artifact targeting is ambiguous and chronologically inverted.** Files to Change says update `scripts/review/results/2026-07-09-plan-701-*.md` — that glob matches the four `round-2-*` files the plan declares "superseded evidence only," contradicting the Artifact Map's narrower `{claude,codex,gemini,disagreement}` set. Additionally, the "current" unsuffixed targets already exist on disk as 0-byte failed-run remnants (`plan-701-claude.md`, `plan-701-codex.md` with `.err` siblings) that are *newer* than the "round-2" files — a naming scheme where "round-2" is older than the unsuffixed current round will mislead any future reader about which evidence is authoritative. The plan mentions `.err` exclusion but never acknowledges the current targets are empty failure artifacts.
6. **Unverifiable claim left as assertion:** "All eight ecosystem repositories were fast-forwarded first. Every fetched digitalmodel remote branch still contains the root `cx-Oracle==6.3.1` pin" (Reproduction proofs) — no command output or branch list is recorded, unlike the other proofs in that section. I could not verify it and it should either carry evidence or be dropped.

Checks run that produced no finding: cx-Oracle/oracledb consumer search (confirmed absent from `src/`/`tests/`/`.github/`/`config/`), workflow matrix/triggers/commands/coverage-gate claims (all exact), archived-docs classification (`plate_capacity_migration.md:17` confirms "not migrated"), `tests/contracts/` and `tests/workflows/workflow_automation/` existence, `workflow-automation` and `__main__` entry points, issue #701 state/decision, hiredis 3.4.0 Windows-wheel availability, `.planning/plan-approved/` gate convention, and lock-level Windows cp312 wheels for the other old pins (lxml, statsmodels, psutil — no evident third blocker).

## Blockers

- Finding 1: justify or reverse the keep-and-freeze decision for hiredis. Either remove it under the same unused-dependency criterion as cx-Oracle (preferred — nothing at root can consume it without `redis`, which is not a dependency), or record in the plan and in #701 an explicit owner decision for why an unconsumed C-extension pin is upgraded and contract-enforced instead of removed. As written, the contract tests would make the wrong state permanent.
