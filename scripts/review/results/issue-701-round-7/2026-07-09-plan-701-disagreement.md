# Disagreement report — plan #701 (2026-07-09)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MINOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=1: no non-interactive gemini auth configured (GEMINI_API_KEY/GOOGLE_API_KEY/~/.gemini/oauth_creds.json) ) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **Trigger-path assertion is underspecified.** Plan §Workflow contract line 311: "assert push and pull_request paths include source, tests, workflow, pyproject, lock" — "source" and "tests" are ambiguous between the existing narrow globs (`src/digitalmodel/workflows/workflow_automation/**`, `tests/workflows/workflow_automation/**` at `.github/workflows/workflow-automation-tests.yml:6-12`) and broad `src/**`/`tests/**`. An implementer reading "source" as broad silently expands when this workflow runs for the whole repo; reading it narrow makes the pseudocode wording wrong. The contract test freezes whatever interpretation is chosen, so the exact glob list must be stated in the plan before implementation. Same ambiguity infects TDD row `test_dependency_changes_trigger_workflow` ("both events include all contract paths").
- **Round-7 namespace already contains artifacts from a prior failed attempt, and the promotion rule doesn't cover staleness.** Plan line 14 covers "zero/missing files observed by a provider during its own run" but `scripts/review/results/issue-701-round-7/` already holds `2026-07-09-plan-701-claude.md.err` and `2026-07-09-plan-701-codex.md.err` plus non-err provider files predating this fanout. If any of those were produced against an earlier plan revision, the "revision-isolated final evidence" property claimed in §Files to Change (round-7 row) is not guaranteed by the stated validation ("fanout completion + non-empty validation"). Validation should also require artifacts postdate the reviewed plan revision or that stale/err files are purged before promotion.
- **README scope in §Files to Change omits the existing Windows build-dependency caveat.** `README.md:80-84` tells Windows users to "install equivalents of those packages or use a pre-built `lxml` wheel" (build-essential/libxml2 guidance). The plan's Deliverable claims compiler-free Windows 3.11/3.12 installs, and my empirical check shows `lxml==4.9.3` now resolves to a cp312 Windows wheel — but the plan's README change reason ("align Python requirement and document base/solver installs") does not reconcile or re-verify this adjacent guidance, leaving the README self-contradictory with the new deliverable claim.
- **The "next-blocker" audit proved only hiredis, not the deliverable.** Plan lines 116–126 test `hiredis` in isolation; the Deliverable ("installs without an MSVC/VC toolchain on supported Windows Python 3.11/3.12") covers a 464-package resolution. This gap is mitigated: step 4 gates the CI change on full disposable installs, and my independent sampling of every remaining exact-pinned C-extension direct dependency (lxml, statsmodels, pymssql, pyarrow, scikit-learn, pillow, h5py, brotli, newrelic, psutil) passed compiler-free on Python 3.12 — but transitive packages remain unproven until step 4 executes, so the Deliverable should be read as conditional on step 4, not established by the plan's evidence.
- **Live-GitHub and drive-search evidence is unverifiable from a `gh`-less host.** Plan §Embedded verification evidence asserts issue labels/comment counts and a drive-index search result that cannot be independently checked in this environment (no `gh`, no connected GitHub app in this session). Everything locally checkable that anchors those claims (squash commit `41289638`, 93 remote refs, file citations) verified exactly, so these are assessed plausible-but-unverified rather than wrong; the plan's own multi-session contract (§Multi-session coordination) correctly fails closed on hosts with this capability gap.
- Checks run with no finding: all 17 referenced paths exist; every line-level citation (`pyproject.toml:10/42/55`, `README.md:90`, workflow matrix/paths/commands/coverage gate, `uv.lock` package + direct-edge entries, archived-doc Oracle text, `enable_redis=True` defaults) is exact; the no-consumer greps reproduce; the RED verification command's conftest chain (`tests/conftest.py` → `tests/contracts/conftest.py` → sibling `assetutilities` import) works in an isolated pytest/pyyaml/packaging environment given the preflighted sibling; hiredis 2.2.3 cp311-Windows-wheel/no-cp312-wheel claim confirmed from the lock's own wheel list and by live install; `yaml.BaseLoader` for the `on` key is correct.

### codex

- **MAJOR — The workflow contract can disable its own enforcement.** Plan §Workflow contract lines 311–320, Implementation Step 5 lines 386–389, and Acceptance Criterion lines 507–508 claim workflow-only changes execute the dependency contract. Both the trigger and the contract invocation reside in `.github/workflows/workflow-automation-tests.yml`; a workflow-only change can remove its own path entry and contract step, causing the workflow to be skipped. No independent guard runs the new file: `.github/workflows/quality-gates-by-domain.yml` runs only three existing harness tests, and `scripts/ci/detect_touched_domains.py:51-60` does not map this workflow to the contracts domain. GitHub evaluates changed files against configured path filters, while `pull_request` workflows execute from the PR merge context. [GitHub path-filter documentation](https://docs.github.com/en/actions/how-tos/write-workflows/choose-when-workflows-run/trigger-a-workflow), [GitHub pull-request event documentation](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows). Put the contract in an independent always-running harness or remove the self-selecting path filter.
- **MAJOR — The metadata contract omits dependency surfaces that can reintroduce the forbidden packages.** Plan §Dependency contract lines 288–299 scans only `[project].dependencies` and `[project.optional-dependencies]`. `pyproject.toml:402-404` also has `[dependency-groups].dev`, while `pyproject.toml:1-2` has build requirements. The frozen workflow install, `uv pip install --no-sources -e ".[dev]"`, installs the optional extra, not the PEP 735 dependency group, and the workflow does not run `uv lock --check`. Consequently, adding `oracledb` or `hiredis` to the dev group while leaving `uv.lock` stale passes every proposed target-workflow assertion. Scan all dependency-bearing tables, including nested dependency-group includes, and enforce lock freshness in CI.
- **MAJOR — The full-suite comparison still has no reproducible baseline.** Plan §Verification lines 449–454 and Acceptance Criterion lines 509–510 refer to a “current-main baseline tracked by #700.” `docs/plans/2026-06-11-issue-700-ci-baseline-domain-gates.md:28-30` records only failing shard names and workflow run IDs—not the canonical command’s nodeids, signatures, environment, or report artifact. Current main cannot produce that Windows baseline with the canonical command because the plan’s own reproduction shows sync failing before pytest. `pyproject.toml:96` also enables `pytest-randomly` without a fixed seed. Define an exact main SHA, OS/Python, fixed seed, machine-readable report, signature normalization, and a same-environment post-change comparison; otherwise “no new failures” remains subjective.
- **MINOR — The hiredis runtime rationale is false for the locked graph.** Plan Resource Intelligence lines 33–38 claims hiredis is “indirectly consumed” and removal may reduce parser throughput. `uv.lock` resolves `redis==7.2.0` with `hiredis==2.2.3`; empirical import returned `redis.utils.HIREDIS_AVAILABLE=False` because Redis 7.2 requires hiredis 3.2 or newer. The current pin is installed but not selected, so the stated throughput risk and performance-reintroduction rationale are unsupported.
- **MINOR — The mandatory central-index change has no landing path.** Plan §Files to Change includes `../workspace-hub/docs/plans/README.md`, but that row is currently an uncommitted modification in a separate Git repository. Implementation Step 8 only pushes the digitalmodel branch and opens its PR, which cannot contain the sibling-repository change. Specify a separate workspace-hub commit/push and ownership sequence.
- **MINOR — The disposable “clean” test run leaks a sibling source checkout.** Plan Verification lines 472–476 runs pytest from this repository, while `tests/conftest.py:51-53` inserts `../assetutilities/src` ahead of the environment installed with `--no-sources`. On the evidence host, workflow tests therefore use the sibling assetutilities source rather than the publishable dependency resolution the clean install is meant to validate. Run this proof in a clone without the sibling override or explicitly verify the imported assetutilities path.

### gemini

- (none)

