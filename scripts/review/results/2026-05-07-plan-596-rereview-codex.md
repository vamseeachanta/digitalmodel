# Re-review artifact: digitalmodel #596 revised repo-structure plan — reviewer 2

Verdict: MAJOR

Findings:
- [severity: high] B1528 evidence move currently requires package-source edits that the plan says are out of scope — the three naval-architecture generators hardcode GitHub blob links to `outputs/b1528_sirocco/**` in source (`src/digitalmodel/naval_architecture/b1528_sirocco_yaw_report.py:147-149`, `b1528_sirocco_time_trace.py:354-356`, `b1528_sirocco_moored_current_report.py:282-283`). If committed evidence is moved to `docs/examples/b1528-sirocco/generated-reports/**`, those source traceability links become stale unless `src/digitalmodel/**` is edited. But the plan says no package-source work is authorized and frames the deliverable as avoiding package-source changes. Concrete required change: explicitly authorize these exact source-link edits in scope, add dedicated verification for updated `traceability_links`, or remove the move from this issue and defer it to a separate approved source+docs migration issue.
- [severity: high] The plan over-classifies all 18 tracked B1528 files as “durable generated report evidence” without a per-file retention rule — current evidence supports durable use of the human-facing report links, and docs also quote some `results.json` values, but the plan does not justify why every CSV/JSON/provenance/manifest file should live under a durable docs-style path. That is a safety problem for the migration matrix because it can silently convert runtime artifacts into permanent repo assets just by moving them. Concrete required change: split the B1528 bundle into explicit subtypes with owner/reason/destination per subtype (for example: curated report pages, machine-readable evidence needed for cited values, and regeneration/provenance artifacts), and state which files remain in-repo versus externalized or regenerated.
- [severity: medium] The stale-reference gate is too broad and the proposed verification does not actually cover the new destination — the plan requires `rg -F 'outputs/b1528_sirocco'` to go clean after migration, but repo docs currently contain legitimate regeneration/output-path instructions and mixed runtime destinations (`../acma-projects/...` in yaw/time-trace docs, `outputs/b1528_sirocco/...` in moored-current docs). Also `mkdocs build --strict` will not validate `docs/examples/**` because `mkdocs.yml` sets `docs_dir: docs/api`. Concrete required change: replace the zero-match rule with scoped checks for stale committed blob/traceability links only, explicitly allow documented runtime output paths where intended, and add a path-existence/reference test for the new `docs/examples/**` location since the docs build will not exercise it.

Approval readiness: not ready
