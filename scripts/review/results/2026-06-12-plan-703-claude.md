Verdict: **MAJOR**

Findings:

- **[MAJOR] §"`pyproject.toml` classifier" — vacuous-truth fail-open hole.** The scoped conditions are all universally-quantified: "the only semantic difference is under `[tool.setuptools.package-data].digitalmodel`", "every changed package-data entry…", "every translated package path…". A comment-only or formatting-only `pyproject.toml` edit parses to *equal* TOML on both sides, so every condition is vacuously true → classified "scoped" with zero changed entries → contributes zero domains. If `pyproject.toml` is the only changed file, the detector emits an empty matrix, and the workflow's `if: needs.detect-domains.outputs.has-domains == 'true'` gate (`.github/workflows/quality-gates-by-domain.yml`) then skips **all** domain tests — fail-open, the exact opposite of fail-closed. The plan's own Risks section contradicts the spec ("may remain full-matrix if the implementation cannot prove a scoped … change") — the spec as literally written *does* "prove" it via vacuity. The spec must require at least one changed entry and define equal-parse behavior explicitly, with a test.

- **[MAJOR] §"Proposed Design" item 4 / Acceptance Criteria — domain mapping never audited against actual cross-domain test importers.** `digitalmodel.solvers.orcaflex` is imported by tests owned by three domains *outside* the planned routing: `contracts` (`tests/test_restructure_compat.py:101-103` asserts these CLI entry-point module paths resolve; also `tests/test_validation_utils.py`), `misc` (`tests/test_engine.py:207` does a real `from digitalmodel.solvers.orcaflex.output_control import OutputController`, plus `simple_engine_test.py`, `additional_engine_tests.py`, `test_engine_isolated.py`), and `infrastructure-other` (`tests/test_level1_minimal.py`, `tests/coverage_analysis.py`). `digitalmodel.orcaflex` is likewise imported by contracts' `test_restructure_compat.py`. Under the planned routing, a module move/rename in `src/digitalmodel/solvers/orcaflex/` runs only `orcaflex-solver` and merges green while breaking contracts/misc/infrastructure tests. The issue AC mandates the narrow mapping, so this may be an accepted trade — but the plan's claim that "routing can target them independently" (Resource Intelligence) was asserted without an importer audit, and the Risks section is silent on it. The plan must either widen the mapping (e.g. `solvers/orcaflex/` → `orcaflex-solver` + `contracts` + `misc`) or record explicit user acceptance of the coverage gap.

- **[MINOR] §"Git diff semantics" — "That preserves fail-closed CI behavior" misstates current behavior.** Today `git_changed_files` uses `check=True` (`scripts/ci/detect_touched_domains.py:74-81`); a git failure raises, `main` returns exit 2, the detect step fails under Actions' default `bash -e`, and `domain-tests-required` hard-fails with "Domain detection did not succeed". The plan converts that loud failure into a silent exit-0 full-matrix run — still fail-closed in effect, but a behavior change that hides misconfigured SHAs forever. The plan should name it as a change and require a stderr diagnostic.

- **[MINOR] §"`pyproject.toml` classifier" — base blob ref unspecified.** With merge-base diff semantics, the base TOML blob must be read from `merge_base:pyproject.toml`, not from the `base` ref. Reading from `base` compares against unrelated base-branch-side commits and diverges from the changed-files list. The plan says only "both base and head TOML blobs" — pin it to merge-base.

- **[MINOR] §"TDD Test List" — spec branches without tests.** The classifier spec enumerates: mixed package-data + non-package-data diffs, globs with `..`/absolute/empty prefix, and (implicitly) package-data keys other than `digitalmodel` (setuptools permits subpackage keys like `"digitalmodel.orcaflex"`), plus the equal-parse/comment-only edge from the first finding and removed-entry-only diffs. None of these has a named test; only dependency-change, unmapped-prefix, invalid-TOML, and unreachable-ref do.

- **[MINOR] §"Verification Commands" / Implementation step 7 — nonexistent script.** `scripts/legal/legal-sanity-scan.sh` does not exist in this checkout (`find scripts -iname "*legal*"` returns nothing), so step 7 and the last verification command cannot run as written. The first command also uses bare `python`, against the workspace's `uv run` convention for Linux.

- **[MINOR] §"Proposed Design" steps 5/7 — classifier plumbing underspecified.** `is_full_matrix_trigger(path)` and `touched_domains(changed_files, domains)` are pure path functions with no access to base/head refs, which the pyproject classifier requires. The plan doesn't specify the signature change or where classification happens relative to the `any(is_full_matrix_trigger(...))` short-circuit at `detect_touched_domains.py:104`, leaving room for `pyproject.toml` to be handled twice (removed from `FULL_MATRIX_PATHS` per step 7 yet still hitting the `src/`-prefix-style escalation path) or for the scoped result to be dropped.

Checked:

- `scripts/ci/detect_touched_domains.py` — confirmed `pyproject.toml` in `FULL_MATRIX_PATHS` (line 24), single-value `SOURCE_DOMAIN_PREFIXES` covering exactly `cathodic_protection`/`citations`/`workflows` (lines 33-37), raw two-dot `git diff base head` with `check=True` → exit 2 on failure (lines 74-81, 167-169), per-path `is_full_matrix_trigger` semantics matching the plan's reproduction table (orcaflex→True, solvers/orcaflex→True, workflows/agents/orcaflex→False, pyproject→True, workflow yml→True).
- `tests/scripts/test_detect_touched_domains.py` — confirmed temp-git-repo style; confirmed `test_unmapped_change_outputs_empty_matrix` proves empty-matrix output is reachable (feeds the fail-open in finding 1).
- `tests/DOMAINS.md` — confirmed `orcaflex`, `orcaflex-solver`, `workflows` domain names and roots, plus `contracts`/`misc`/`infrastructure-other` ownership of the cross-importing top-level test files.
- `.claude/quality-gates.yaml` — confirmed `tests-orcaflex`, `tests-orcaflex-solver`, `tests-workflows` gates exist independently.
- `pyproject.toml` lines 227-228 — confirmed `[tool.setuptools.package-data]` with single `digitalmodel` key; current globs (`subsea/...`, `naval_architecture/...`) are *not* in the planned source map, so today's-file-shaped edits fail closed as designed.
- `.github/workflows/quality-gates-by-domain.yml` — confirmed `fetch-depth: 0` (merge-base feasible in CI), Python 3.11 (`tomllib` available), PR invocation passes `base.sha`/`head.sha` (no workflow edit needed — no scope creep), and the `has-domains` skip path.
- Importer sweep: `grep -rln digitalmodel\.(solvers\.)?orcaflex tests/` and `workflows.agents.orcaflex` — basis for finding 2; verified `tests/test_engine.py:207` is a real import, not just a mock list, and `test_restructure_compat.py:101-103` resolves solver CLI module paths.
- Issue #703 body — plan's acceptance criteria and the narrow mappings transcribe the issue faithfully; scope exclusions (#704/#705/#706) match. Approval Gate section complies with the adapter's plan-approval gate; issue is `status:pending`, no self-approval.
- `docs/plans/` — confirmed `README.md`/`_template-issue-plan.md` absent and standalone plan-file pattern, as the plan claims.
