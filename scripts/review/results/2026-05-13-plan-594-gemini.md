# Plan Review Artifact — digitalmodel #594 — Gemini

Prompt: `.planning/quick/review-594-plan-prompt.md`
Raw log: `.planning/quick/review-594-gemini.out`

---

Ripgrep is not available. Falling back to GrepTool.
**Verdict:** MAJOR

### CRITICAL
*(None)*

### HIGH
**Risk of unmitigated downstream breakage**
- **Cite:** Task 2, "Rename or reframe `gtm_storyline` to `reference_insights` unless this would break too many existing demo consumers"
- **Risk:** The plan proposes a conditional rename of a key in `examples/demos/gtm/data/ctv_operability_kincardine.json` but provides no task or shell command to actually search the codebase for consumers. Relying on the developer's intuition to know if it "would break too many existing demo consumers" without a systematic check is a high risk of silently breaking other demo scripts or tests that expect `gtm_storyline`.
- **Fix:** Add a preliminary `grep -rn "ctv_operability_kincardine.json"` or `grep -rn "gtm_storyline" examples/demos/` command to Task 2 to definitively identify consumers. Require updating those consumers if the key is renamed.

### MEDIUM
**Verification scope is too narrow**
- **Cite:** Task 5, `uv run pytest examples/demos/gtm/tests/test_ctv_operability_reference.py -q`
- **Risk:** Modifying data in a shared `gtm/data/` directory and renaming fields (like `_description` and `gtm_storyline`) could break other integration tests, pipeline scripts, or fixture loaders that iterate over all JSON files in the data directory. Running only the targeted test file is insufficient to prove no regressions occurred.
- **Fix:** Append a broader test suite execution command to Task 5, such as `uv run pytest examples/demos/` or simply `uv run pytest`, to guarantee the metadata changes do not cause cross-file regressions.

### LOW
**Misleading test function names left behind**
- **Cite:** Task 1, "Modify: examples/demos/gtm/tests/test_ctv_operability_reference.py ... Test intent: replace assertions that require GTM/prospect-facing with assertions that require: data['_source']['usage_boundary']"
- **Risk:** The existing test file contains a function named `test_ctv_operability_supports_gtm_vessel_suitability_storyline`. If the plan only changes the *assertions* inside the test, the test's name will continue to propagate the GTM/storyline framing, conflicting with the intent of the reference-only boundary.
- **Fix:** Explicitly state in Task 1 that test function names must be renamed to reflect their new boundary-enforcing purpose (e.g., rename to `test_ctv_operability_enforces_internal_reference_boundary`).

### What I Checked
1. Read `examples/demos/gtm/tests/test_ctv_operability_reference.py` to confirm existing test names and assertions.
2. Read `examples/demos/gtm/data/ctv_operability_kincardine.json` to verify the presence of `gtm_storyline`, `demo_use_cases`, and `_description`.
3. Validated the plan's alignment with the usage boundaries established in `references/vessel-suitability/README.md` and `docs/session-handoffs/2026-05-13-ctv-operability-reference-boundary.md`.
4. Evaluated the completeness and safety of the verification commands in Tasks 1 and 5.
