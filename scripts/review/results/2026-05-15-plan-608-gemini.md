## Verdict
APPROVE

## Retrieval
- Read `docs/plans/2026-05-15-issue-608-orcawave-mesh-quality-gates.md` (the plan).
- Read `AGENTS.md` to verify `test_command` and module routing.
- Read `src/digitalmodel/hydrodynamics/diffraction/cli.py` (lines 400-590) to verify existing Click commands (`convert-spec`, `validate-spec`, `run-orcawave`).
- Read `src/digitalmodel/hydrodynamics/diffraction/spec_converter.py` (lines 1-180) to verify `SpecConverter.validate()` logic.
- Read `src/digitalmodel/hydrodynamics/diffraction/orcawave_runner.py` (lines 220-560) to verify `prepare()` and `_validate_mesh_references()` behavior.
- Read `src/digitalmodel/hydrodynamics/diffraction/mesh_pipeline.py` (lines 1-280) to verify `MeshPipeline.load()` and `validate()` delegation.
- Read `src/digitalmodel/hydrodynamics/diffraction/geometry_quality.py` (entire file) to verify `GeometryQualityChecker` implementation gaps (tri-only indexing) and `GeometryQualityReport` fields.
- Read `src/digitalmodel/hydrodynamics/bemrosetta/mesh/mesh_handler.py` to verify `BaseMeshHandler.validate_mesh()` reliably populated fields.
- Read `src/digitalmodel/hydrodynamics/bemrosetta/models/mesh_models.py` to verify `PanelMesh` and `MeshQualityReport` dataclass fields.
- Read `src/digitalmodel/hydrodynamics/diffraction/input_schemas.py` to verify `FrequencySpec.to_frequencies_rad_s()` and `EnvironmentSpec.water_depth` validation logic.
- List `src/digitalmodel/hydrodynamics/bemrosetta/mesh/` to verify existence of `dat_handler.py`, `gdf_handler.py`, `stl_handler.py`.
- List `docs/plans/` in both `digitalmodel` and workspace root to verify existence of dependency plans (#500, #605, #606, #609).
- Grep workspace for `#500` to confirm its status as plan-approved but local artifact as draft.
- Glob `src/digitalmodel/hydrodynamics/diffraction/mesh_preflight.py` to confirm it does not exist at HEAD (verified dependency block).

## Findings
1. **Plan §Sources consulted** claim that `GeometryQualityChecker` and `GeometryQualityReport` already cover watertightness, normals, etc., but identifies gaps in quad handling. I verified in `src/digitalmodel/hydrodynamics/diffraction/geometry_quality.py` lines 124, 224, 267 that the current implementation indeed uses `panel[:3]`, which ignores the 4th node of quad panels. The plan's Task 1 correctly addresses this by proposing to drop `-1` sentinel padding and handle 3 and 4 node panels.
2. **Plan §Issue-specific code findings** claim that `SpecConverter.validate()` does not load mesh files. I verified in `src/digitalmodel/hydrodynamics/diffraction/spec_converter.py` lines 147-178 that `validate()` only performs semantic checks on metadata and presence of strings, confirming the plan's assessment of the current state.
3. **Plan §Gaps identified** claim that #608 implementation is blocked by #500/#605/#606. I verified via `glob` that `mesh_preflight.py` and `orcawave_asset_resolver.py` do not yet exist in `src/digitalmodel/hydrodynamics/diffraction/`. The plan correctly identifies this as a blocker in §Risks and Open Questions and §Proposed Tasks line 96.
4. **Plan §Proposed Tasks line 72** proposes adding `max_panel_edge`, `quad_nonplanarity_ratio`, and `bbox_characteristic_length` to `GeometryQualityReport`. I verified in `src/digitalmodel/hydrodynamics/diffraction/geometry_quality.py` that these fields are currently missing, making the task necessary for the proposed OrcaWave quality gates.
5. **Plan §Proposed Tasks line 90** correctly identifies that normal consistency blocking should be deferred. I verified in `src/digitalmodel/hydrodynamics/bemrosetta/mesh/mesh_handler.py` lines 167-195 that the current `_check_normal_consistency` is a "simplified heuristic" using a mesh center dot product, which would be prone to false positives for complex geometries.
6. **Plan §Pseudocode line 122** invokes `GeometryQualityChecker(...).generate_report(str(asset.path), mesh.vertices, mesh.panels, verbose=False)`. I verified in `src/digitalmodel/hydrodynamics/diffraction/geometry_quality.py` line 291 that `generate_report` currently lacks a `verbose` parameter and always prints to stdout. The plan's Task 1 includes adding this parameter to support quiet CLI/JSON output, which is a correct and necessary refactor.

## Blockers
None. (The plan correctly identifies its own external dependencies in #500, #605, and #606 and includes pre-implementation import/API gates to enforce them).
