Verdict: MAJOR
Findings:
- [MAJOR] docs/plans/2026-06-11-issue-700-ci-baseline-domain-gates.md:156-158: PR detection is left on the current base/head semantics, but `scripts/ci/detect_touched_domains.py:74-81` runs `git diff --name-only base head`, not merge-base/three-dot. A PR behind `main` can include base-only changes and select unrelated domains. Required fix: define separate PR vs push diff semantics, use merge-base/three-dot for PRs, keep before/after for pushes, and add a regression test where `main` advances in an unrelated domain after the branch point.
- [MAJOR] docs/plans/2026-06-11-issue-700-ci-baseline-domain-gates.md:27,160-185: the plan narrows the target PR to `citations`/`orcaflex`, but its own evidence says `tests-orcaflex` and `tests-orcaflex-solver` are failing; `.claude/quality-gates.yaml:136-152` makes those selected shards blocking. Required fix: add explicit implementation tasks and verification for selected-domain failures, or revise acceptance so the plan does not claim PR unblock while selected shards remain red.
- [MAJOR] docs/plans/2026-06-11-issue-700-ci-baseline-domain-gates.md:138,158-159,183,203-205: “non-blocking full baseline handling” is a deliverable and acceptance criterion, but the workflow design question is still open and current `.github/workflows/quality-gates-by-domain.yml:115-133` has one aggregate job that fails whenever selected domain jobs fail. Required fix: decide the check-name/job split before implementation, specify how scheduled/manual full sweeps stay diagnostic without affecting required PR checks, and add workflow validation for those event branches.
- [MINOR] docs/plans/2026-06-11-issue-700-ci-baseline-domain-gates.md:120,169-176,187-192: scope says to cover “other top-level package/domain mappings,” but the test list only names OrcaFlex, OrcaFlex solver, pyproject cases, and existing detector tests. Required fix: enumerate the exact source-prefix map additions and add existence/drift tests against `tests/DOMAINS.md` and `.claude/quality-gates.yaml`, or narrow scope to OrcaFlex only.

Checks performed:
- Read the draft plan with line numbers.
- Inspected `.github/workflows/quality-gates-by-domain.yml`, `scripts/ci/detect_touched_domains.py`, `tests/scripts/test_detect_touched_domains.py`, `.claude/quality-gates.yaml`, `tests/DOMAINS.md`, `pyproject.toml`, and `tests/workflows/automation/test_quality_gates.py`.
- Verified current classifier still treats `pyproject.toml`, `src/digitalmodel/orcaflex/...`, and `src/digitalmodel/solvers/orcaflex/...` as full-matrix triggers.