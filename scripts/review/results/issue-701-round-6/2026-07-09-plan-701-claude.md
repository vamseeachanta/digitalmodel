## Verdict

MAJOR

## Retrieval

- Read `docs/plans/2026-07-09-issue-701-oracle-dependency-isolation.md` in full (532 lines).
- Read `pyproject.toml` lines 1–120 and grepped lines 146–193 (optional-dependencies): confirmed `requires-python = ">=3.11"` at line 10, `cx-Oracle==6.3.1` at line 42, `hiredis==2.2.3` at line 55, and that the `dev` and `solvers` extras referenced by the plan's commands exist.
- Read `README.md` lines 80–104: line 90 says "Requires Python 3.10+." as claimed.
- Read `.github/workflows/workflow-automation-tests.yml` in full: matrix is `[ubuntu-latest] × ['3.11','3.12']`; push paths include the workflow file but omit `pyproject.toml`/`uv.lock`; PR paths omit all three — exactly as the plan states. The four named commands and the Ubuntu/3.11 coverage guard match verbatim.
- Grepped `uv.lock`: `cx-oracle` 6.3.1 (line 1263, sdist-only, no wheels), `hiredis` 2.2.3 (line 2570, cp311 win32/win_amd64 wheels present, zero cp312 wheels), both listed as direct dependencies of the editable `digitalmodel` package (lines 1398, 1414) and in its `requires-dist` (1596, 1619); `fastapi-limiter` → `redis` edge confirmed (lines 1990–1996).
- Checked cp312 win_amd64 wheel coverage in the lock for every other exact-pinned C extension the plan doesn't mention: `pymssql 2.3.8`, `lxml 4.9.3`, `brotli 1.1.0`, `pillow 10.1.0`, `pyarrow 14.0.1`, `scikit-learn 1.3.2`, `h5py 3.10.0`, `pyproj 3.6.1`, `psutil 5.9.6` (abi3) — all have Windows 3.12 wheels, so the two removals plausibly are the only Windows blockers at locked versions.
- Ripgrep for `cx[_-]?oracle|oracledb` and `hiredis` across `src/`, `tests/`, `.github/`, `config/`: matches only in gitignored `src/digitalmodel.egg-info/` (see Finding 3) and the dashboard `requirements.txt` pins the plan already scopes out.
- `git show --stat 41289638`: PR #699 squash commit does modify `.github/workflows/workflow-automation-tests.yml` (15 lines) — the plan's provenance claim for the Ubuntu-only matrix holds.
- Verified file-existence table: `tests/contracts/` (exists, `test_dependency_isolation.py` missing), the three cache test files, both `cache.py` files (`enable_redis: bool = True` at line 27 in each), dashboard `requirements.txt` lines 17–18, `docs/session-handoffs/` (handoff file not yet created), `../llm-wiki/coordination/HANDOFF_TEMPLATE.md`, `../assetutilities/src`, `.planning/plan-approved/` — all as claimed.
- Read `tests/conftest.py`, `tests/contracts/conftest.py`, `pytest.ini`, and `../assetutilities/src/assetutilities/__init__.py`: the contracts conftest requires `assetutilities` at collection time; the parent conftest adds `../assetutilities/src` to `sys.path`, and the package defines `__version__` with no third-party imports — so the plan's step-1 sibling preflight guards the right condition for the isolated RED command.
- Listed `scripts/review/results/issue-701-round-6/` and read its contents and `.err` files (see Findings 1–2). Listed `issue-701-round-5/` (all four files present).
- NOT verified: live GitHub state. `gh` is not installed on this host and no GitHub connector is available to this session, so issue #701's state/labels/comment count and #699's MERGED status are taken from the plan's embedded evidence only; I verified the squash commit locally instead.

## Findings

1. **Plan §Files to Change line 328 makes a false claim about the round-6 evidence paths.** It says the round-6 files are to be created and "absence during provider execution is expected" — but `scripts/review/results/issue-701-round-6/` already exists and contains contaminated artifacts from a failed prior attempt: zero-byte `2026-07-09-plan-701-claude.md` and `...-codex.md`, two `.err` logs, a `...-gemini.md` whose body is a stale `UNAVAILABLE` verdict, and no disagreement file. The plan (line 12, "Current review targets") points the approval gate at paths that currently hold garbage, with no cleanup or renumbering step. Anyone aggregating round 6 could read the stale gemini `UNAVAILABLE` or the zero-byte provider files as this round's results.

2. **The review harness the Approval Gate depends on demonstrably fails on this host, and the plan doesn't address it.** `...claude.md.err` shows the Claude CLI ran with "workspace has not been trusted", ignoring 19 permission entries and producing a zero-byte review; the Codex invocation wrote its entire session transcript to `...codex.md.err` while leaving `codex.md` empty. Plan §Approval Gate (lines 527–531) requires "two usable no-MAJOR plan reviews", and §Adversarial Review Summary line 488 says "A fresh round is required" — but the plan records only Gemini's auth limitation (line 494) and is silent on the Claude trust and Codex output-redirection failures that made the last round produce zero usable reviews. Without remediation steps (trust the workspace or run on a working host; fix the codex stdout/stderr routing), the gate cannot be satisfied as written.

3. **Plan lines 45–46 ("A case-insensitive search of `src/` … finds no `cx_Oracle`") is literally false for the working tree.** `src/digitalmodel.egg-info/requires.txt:26` and `src/digitalmodel.egg-info/PKG-INFO:45` contain `cx-Oracle==6.3.1` (and lines 43/62 contain `hiredis==2.2.3`). The plan's `rg` proof (line 209) returns no matches only because `*.egg-info/` is gitignored (`.gitignore:90`) and ripgrep skips it. Harmless generated files, but the claim should be scoped to tracked files, and stale egg-info will still list cx-Oracle after removal — a post-implementation grep audit that doesn't respect gitignore will "rediscover" the pin.

4. **The coordination contract's execution path is unverified on the evidence host.** Plan line 400 says "GitHub reads/writes use the connected app on hosts without `gh`" — on this host (the same `ACMA-HOU-RDS02` that produced the reproduction proofs), `gh` is not installed and no GitHub connector is available to this session. The claim/renew protocol (lines 386–391) requires posting to #701 at least every 20 minutes; if the implementing session on this host has neither `gh` nor a connected app, the mandatory coordination contract is unexecutable, and the plan defines no fallback beyond the one-line assumption.

5. **The workflow contract (§Workflow contract, lines 299–311) freezes the install/pytest/CLI/import commands and the coverage guard but not the "Create virtual environment" step (`uv venv --python "${{ matrix.python-version }}"`, workflow lines 35–37).** The frozen "Install dependencies" command (`uv pip install --no-sources -e ".[dev]"`) only works because that prior step creates the target venv; a future edit removing or altering the venv step would pass every asserted contract while breaking CI.

Claims checked and **not** found defective: all cited line numbers in `pyproject.toml`/`README.md`; the workflow matrix, trigger-path gaps, and exact commands; both packages' direct/requires-dist edges and versions in `uv.lock`; hiredis 2.2.3 having cp311 but not cp312 Windows wheels; cx-Oracle 6.3.1 being sdist-only; the "no other Windows 3.12 wheel blocker" implication (verified against every remaining exact-pinned C extension in the lock); commit 41289638 touching the workflow; the sibling-assetutilities preflight matching real conftest collection-time behavior; existence of the `dev`/`solvers` extras, cache modules with `enable_redis=True` defaults, all referenced test files, the handoff template, and `.planning/plan-approved/`.

## Blockers

- Finding 1: purge or explicitly disposition the failed `issue-701-round-6/` stubs (including `.err` files) before designating those paths as the current review targets, or renumber the round; the plan text asserting their absence must be corrected.
- Finding 2: the plan must state how the fresh review round will actually produce usable Claude/Codex outputs given the recorded trust and output-routing failures — otherwise its own approval gate is unsatisfiable and implementation can never legitimately start.
