# Disagreement report — plan #701 (2026-07-09)

## Verdicts

| Provider | Verdict |
|---|---|
| claude | MAJOR |
| codex | MAJOR |
| gemini | UNAVAILABLE (gemini CLI failed, rc=1: no non-interactive gemini auth configured (GEMINI_API_KEY/GOOGLE_API_KEY/~/.gemini/oauth_creds.json) ) |

## Findings unique to each provider

A finding is 'unique to X' if its text appears in X's artifact but not
verbatim in any other provider's artifact.

### claude

- **Plan §Files to Change line 328 makes a false claim about the round-6 evidence paths.** It says the round-6 files are to be created and "absence during provider execution is expected" — but `scripts/review/results/issue-701-round-6/` already exists and contains contaminated artifacts from a failed prior attempt: zero-byte `2026-07-09-plan-701-claude.md` and `...-codex.md`, two `.err` logs, a `...-gemini.md` whose body is a stale `UNAVAILABLE` verdict, and no disagreement file. The plan (line 12, "Current review targets") points the approval gate at paths that currently hold garbage, with no cleanup or renumbering step. Anyone aggregating round 6 could read the stale gemini `UNAVAILABLE` or the zero-byte provider files as this round's results.
- **The review harness the Approval Gate depends on demonstrably fails on this host, and the plan doesn't address it.** `...claude.md.err` shows the Claude CLI ran with "workspace has not been trusted", ignoring 19 permission entries and producing a zero-byte review; the Codex invocation wrote its entire session transcript to `...codex.md.err` while leaving `codex.md` empty. Plan §Approval Gate (lines 527–531) requires "two usable no-MAJOR plan reviews", and §Adversarial Review Summary line 488 says "A fresh round is required" — but the plan records only Gemini's auth limitation (line 494) and is silent on the Claude trust and Codex output-redirection failures that made the last round produce zero usable reviews. Without remediation steps (trust the workspace or run on a working host; fix the codex stdout/stderr routing), the gate cannot be satisfied as written.
- **Plan lines 45–46 ("A case-insensitive search of `src/` … finds no `cx_Oracle`") is literally false for the working tree.** `src/digitalmodel.egg-info/requires.txt:26` and `src/digitalmodel.egg-info/PKG-INFO:45` contain `cx-Oracle==6.3.1` (and lines 43/62 contain `hiredis==2.2.3`). The plan's `rg` proof (line 209) returns no matches only because `*.egg-info/` is gitignored (`.gitignore:90`) and ripgrep skips it. Harmless generated files, but the claim should be scoped to tracked files, and stale egg-info will still list cx-Oracle after removal — a post-implementation grep audit that doesn't respect gitignore will "rediscover" the pin.
- **The coordination contract's execution path is unverified on the evidence host.** Plan line 400 says "GitHub reads/writes use the connected app on hosts without `gh`" — on this host (the same `ACMA-HOU-RDS02` that produced the reproduction proofs), `gh` is not installed and no GitHub connector is available to this session. The claim/renew protocol (lines 386–391) requires posting to #701 at least every 20 minutes; if the implementing session on this host has neither `gh` nor a connected app, the mandatory coordination contract is unexecutable, and the plan defines no fallback beyond the one-line assumption.
- **The workflow contract (§Workflow contract, lines 299–311) freezes the install/pytest/CLI/import commands and the coverage guard but not the "Create virtual environment" step (`uv venv --python "${{ matrix.python-version }}"`, workflow lines 35–37).** The frozen "Install dependencies" command (`uv pip install --no-sources -e ".[dev]"`) only works because that prior step creates the target venv; a future edit removing or altering the venv step would pass every asserted contract while breaking CI.
- Claims checked and **not** found defective: all cited line numbers in `pyproject.toml`/`README.md`; the workflow matrix, trigger-path gaps, and exact commands; both packages' direct/requires-dist edges and versions in `uv.lock`; hiredis 2.2.3 having cp311 but not cp312 Windows wheels; cx-Oracle 6.3.1 being sdist-only; the "no other Windows 3.12 wheel blocker" implication (verified against every remaining exact-pinned C extension in the lock); commit 41289638 touching the workflow; the sibling-assetutilities preflight matching real conftest collection-time behavior; existence of the `dev`/`solvers` extras, cache modules with `enable_redis=True` defaults, all referenced test files, the handoff template, and `.planning/plan-approved/`.

### codex

- **MAJOR — The proposed contract is not a PR/push enforcement gate for two of the files it claims to guard.** The plan creates `tests/contracts/test_dependency_isolation.py` (`§Files to Change`, lines 323–329), but `.github/workflows/workflow-automation-tests.yml:43-45` runs only `tests/workflows/workflow_automation/`. The general harness at `.github/workflows/quality-gates-by-domain.yml:89-95` also omits it. `scripts/ci/detect_touched_domains.py:18-60,218-245` maps neither `uv.lock` nor `.github/workflows/workflow-automation-tests.yml` to the contracts domain; empirical routing returned `[]` for each file. Consequently, a lock-only or workflow-only regression can pass the immediate PR/push gates; only the later scheduled full sweep would execute the contract. Add an explicit contract-test workflow step or route both paths to `tests-contracts`.
- **MAJOR — The coordination tokens are incompatible with the mandated claim helper.** Plan lines 387–399 specify `🔒 CLAIM ...` and `🔓 RELEASE ...`. `../llm-wiki/scripts/coordination/claim.py:28,58-59,138,161` recognizes and emits only `ASCP CLAIM`/`ASCP RELEASE`; the parser returned `False` for the plan token and `True` for the helper token. The existing #701 claim comment already uses the unrecognized form. This defeats the `claim.py → BLOCKED` requirement in `../workspace-hub/AGENTS.md:11`, allowing another compliant session to acquire the same work. Invoke the helper or use its exact tokens.
- **MAJOR — Full-regression disposition is undefined despite a recorded unresolved baseline.** Plan lines 376–379 require running the canonical suite, but Acceptance Criteria lines 459–476 require only focused tests to pass and do not require the full suite to pass or show no new failures. Live issue [#700](https://github.com/vamseeachanta/digitalmodel/issues/700) remains open and records baseline failures across contracts, workflows, and other shards. This conflicts with `../workspace-hub/docs/plans/README.md:120-126`, which requires confirming no regressions. Define a same-commit baseline comparison or an explicit pass requirement and require every delta to be resolved before closeout.
- **MINOR — The compiler-free proof is not fully replayable and still overclaims.** Plan lines 125–141 summarize an unspecified registry query and call the host “compiler-free”; lines 431–438 replace the required audit with comments and do not identify the fallback clean image. The current host does satisfy the listed MSVC checks, but the plan does not supply executable commands that another session can reproduce, and “compiler-free” is broader than the stated MSVC/VC boundary. Provide the exact PowerShell audit and identify the fallback image.
- **MINOR — The mandatory plan index is omitted for the wrong reason.** Plan lines 59–60 dismiss an index because this repository has no local `docs/plans/README.md`, while the governing `../workspace-hub/docs/plans/README.md:71-76,483-489` requires every new plan to be added to its central index. That index currently has no #701 entry, and the Files to Change table omits it. Add the central index update or document an approved exception.

### gemini

- (none)

