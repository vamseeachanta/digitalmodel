Verdict: MAJOR

Findings:
- [MAJOR] Plan "Artifact Map" / Approach 5–6 / Acceptance criterion 7 — the required-vs-diagnostic context split is built on a false premise and is unimplementable as written. I verified directly: `main` has **no classic branch protection** (`gh api .../branches/main/protection` → 404 "Branch not protected") and the only active ruleset (17369753) contains `deletion`, `non_fast_forward`, and `pull_request` rules — **no `required_status_checks` rule**. So the claim "Required touched-domain contexts will be `Detect touched test domains`, `tests-<domain>`, and `Domain touched aggregate`" (plan line 111) describes enforcement that does not exist, and the plan contains no step to create or update a ruleset. Two consequences: (a) acceptance criteria 7 ("do not share … required contexts") and the aggregate-rename rationale are currently no-ops — `baseline-tests-*` is exactly as non-required as `tests-*`; (b) worse, if an implementer follows line 111 and configures `tests-<domain>` as required contexts, every PR that doesn't select a given domain will hang forever on a never-reporting context, because the matrix is dynamic. Required fix: add an explicit admin step that configures ruleset required status checks as the aggregate only (`Domain touched aggregate`, optionally `Detect touched test domains`), remove `tests-<domain>` from the required list, **or** explicitly state that enforcement is merge-policy convention with no GitHub-level required checks and reword criterion 7 and Approach 5/6 accordingly. The rename itself is fine; the enforcement story is the gap, and it was the centerpiece of the r1/r2 MAJOR fixes.
- [MINOR] Plan "Reproduction proofs" — the catenary traceback cites `src/digitalmodel/orcaflex/mooring_design.py:300: in solve_catenary / h_calc = H / w_kn * (math.cosh(V / H) - 1.0)`, but on this worktree (which contains `origin/main` at 41289638) that statement is at line 246–247 and line 300 is inside the `MooringLineSegment` model. The proof was evidently captured on a non-main tree (most likely the #702 branch, which modifies this file). Since the plan's own scoping rule is "limit selected-shard repairs to failures that reproduce on current `main`" (Risks, line 261), restate the local proofs against a clean `main` checkout or note their provenance; the CI evidence (main run 27375765025 failing the same shards) partially covers this but the local proof as quoted does not match main.
- [MINOR] Plan In Scope / Approach step 4 — overlapping source-prefix semantics are unspecified. The plan adds `src/digitalmodel/workflows/agents/orcaflex/` → {workflows, orcaflex} while `src/digitalmodel/workflows/` → workflows already exists in `SOURCE_DOMAIN_PREFIXES` (scripts/ci/detect_touched_domains.py:36). The current implementation breaks at the first matching prefix in dict iteration order (detect_touched_domains.py:115-118), so the result for the agents path depends on insertion order. Specify union-of-all-matching-prefixes (or longest-prefix-wins) as the contract; the TDD test would catch the wrong outcome but the plan should state the rule, not leave it to the implementer.
- [MINOR] Approach step 4 (`pyproject.toml` classifier) — the mapping from package-data globs to domains needs two unstated details: (1) globs under `[tool.setuptools.package-data].digitalmodel` are package-relative (verified: current values are `subsea/cross_sections/fixtures/*.yml`, `naval_architecture/data/*.yml` at pyproject.toml:228), so they must be translated to `src/digitalmodel/<glob-prefix>/` before hitting the prefix map; (2) the fallback for a package-data glob whose translated prefix is unmapped (both current entries — `subsea/` and `naval_architecture/` — will be unmapped after this plan's narrow additions) must be full matrix. The TDD list has no test for "package-data-only diff with an unmapped package prefix → full matrix"; add it, since that is the realistic main-baseline case today.
- [MINOR] Approach step 5 — only `schedule` is removed from `quality-gates-by-domain.yml`, but `workflow_dispatch` remains (quality-gates-by-domain.yml:10) and falls into the full-mode else-branch (lines 47-52). A manual dispatch on `main` would post red `tests-*` / `Domain touched aggregate` commit statuses from stale broad-matrix failures — exactly the noise the diagnostic split is meant to remove. Remove or redirect `workflow_dispatch` to the diagnostics workflow, or make dispatch use touched/empty mode.

Checks performed:
- Read the full plan and verified every cited artifact path exists: `.github/workflows/quality-gates-by-domain.yml`, `scripts/ci/detect_touched_domains.py`, `tests/scripts/test_detect_touched_domains.py` (r2 path fix confirmed — file exists, 13KB, and `tests/scripts/` is a `workflows` domain root in tests/DOMAINS.md:28, so the bootstrap PR routes to `workflows` as claimed), `.claude/quality-gates.yaml` (gates `tests-orcaflex`:136, `tests-orcaflex-solver`:146, `tests-workflows`:196), `tests/DOMAINS.md` (domains `orcaflex`/`orcaflex-solver`/`workflows` with the roots the plan assumes, including `tests/test_orcaflex_agent.py` under `orcaflex`).
- Verified detector claims against source: `pyproject.toml` and the workflow file are in `FULL_MATRIX_PATHS` (lines 19-25); `src/digitalmodel/orcaflex/` is unmapped in `SOURCE_DOMAIN_PREFIXES` (lines 33-37) so the quoted `is_full_matrix_trigger` outputs are correct; PR mode uses raw `git diff base head` (line 74-81, two-dot), confirming the merge-base fix is needed; CLI flags match step 10's commands.
- Verified workflow claims: push and schedule both fall to `--mode full` (else-branch, lines 47-52); aggregate job is currently named `Domain test aggregate` (line 116); `fetch-depth: 0` (line 23) makes merge-base computable; Python 3.11 (line 28) makes `tomllib` available for the structural TOML parser.
- Verified all 27 TDD-list test IDs: every existing test class/function cited (TestCatenary, TestDAF, TestBaseFileGenerator, TestBuilderRegistry, TestSchemaValidation, mooring_tension_iteration tests, `test_minimal_fixture_report_matches_snapshot`, TestCLIDefaults) exists at the cited paths, including `tests/solvers/orcaflex/reporting/test_fixture_snapshot.py` (initially looked missing due to a truncated listing — re-checked, it exists).
- Verified failure claims statically: `BaseFileGenerator` in `src/digitalmodel/workflows/agents/orcaflex/generators/base_files.py` has no `generate_var_data` (AttributeError claim consistent); the `h_calc` overflow expression exists in `solve_catenary` — but at line 246, not the quoted 300 (finding 2).
- Verified GitHub evidence where reachable: run 27380515825 is `Quality Gates by Domain`, conclusion `failure`; checked classic branch protection (404) and the active branch ruleset rules for `main` (no required status checks) — basis for the MAJOR finding.
- Checked `[tool.setuptools.package-data]` shape in pyproject.toml (key `digitalmodel`, package-relative globs) against the classifier spec in Approach step 4.
- Could not re-run the failing pytest reproductions in this worktree (no `.venv`; building the env was out of proportion for review) — relied on static source verification plus the main-run CI evidence instead.
