# Issue 1602 Plan Review — Round 5 Data/Provenance

## Verdict

MAJOR

## Retrieval

Reviewed pushed commit `8216f567521903066d1bef31e17300ef50c3cacb`; both declared validators passed.

## Findings

1. Public clean-room rows omit signed claim fields, so signatures cannot be independently reconstructed.
2. The normalized reviewer entity omits reviewer-signed allowlist/environment hashes.
3. Current-profile points collide globally when different cases share a depth.
4. Content-addressed component IDs conflict with single-configuration ownership in the release table.
5. `float64` release values conflict with canonical-decimal logical hashing.
6. Nested JCS engineering payloads lack closed schemas for shape, units, axes, and ordering.
7. Connection/boundary lineage lacks concrete public subject tables.
8. Operability rows cannot satisfy the declared quantity/comparison join or recompute margin/utilization/operable.
9. “Every leaf” lineage contradicts the narrower protocol lineage scope.
10. Dynamics bounds, fixed-motion fields, and duplicated pressure/environment equality rules are incomplete.
11. The validator checks tokens/presence rather than these invariants.

## Blockers

The detailed normalized/release schema and executable validator require child-level design and tests rather than further parent-plan prose expansion.
