Verdict: MAJOR

Findings:
- [major] docs/plans/2026-06-11-issue-700-ci-baseline-domain-gates.md section "Approach": The proposed source-prefix routing misses `src/digitalmodel/workflows/agents/orcaflex/`. Since `tests/test_orcaflex_agent.py` (which is in the `orcaflex` domain and is currently failing) tests the code in this directory, changing the agent code will currently only trigger the `workflows` domain tests (via the existing `src/digitalmodel/workflows/` prefix), leaving the critical failing agent test un-executed.
- [minor] scripts/ci/detect_touched_domains.py: The plan to add narrow routing for `pyproject.toml` requires removing it from `FULL_MATRIX_PATHS` and implementing a structural TOML parser (likely using `tomllib` in Python 3.11+). The plan must ensure that any change outside `[tool.setuptools.package-data].digitalmodel`—including dependencies, project metadata, or tool configurations—immediately fails closed to the full matrix.
- [minor] docs/plans/2026-06-11-issue-700-ci-baseline-domain-gates.md section "Risks": The plan does not explicitly address potential cross-domain dependencies between `orcaflex` (models) and `orcaflex-solver` (adapters). While currently appearing independent, changes to core models in `src/digitalmodel/orcaflex/` may impact solver behavior, yet the proposed routing only triggers the `orcaflex` domain.

Checks performed:
- Verified `solve_catenary` overflow issue in `src/digitalmodel/orcaflex/mooring_design.py`.
- Verified `AttributeError` for `generate_var_data` in `tests/test_orcaflex_agent.py` by inspecting `src/digitalmodel/workflows/agents/orcaflex/generators/base_files.py`.
- Verified `pyproject.toml` structure and current `FULL_MATRIX_PATHS` inclusion.
- Verified domain roots in `tests/DOMAINS.md` and their mismatch with proposed `SOURCE_DOMAIN_PREFIXES` for agents.
- Confirmed `digitalmodel.orcaflex` redirect logic in `src/digitalmodel/_compat.py` and its impact on test imports.
- Inspected `.github/workflows/quality-gates-by-domain.yml` for current push/PR logic.
